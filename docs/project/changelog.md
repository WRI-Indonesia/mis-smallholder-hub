# Proyek — Changelog & Decision Log

> Bagian dari dokumentasi **Proyek**. Indeks: [../README.md](../README.md) · Terkait: [brief.md](./brief.md) · [roadmap.md](./roadmap.md) · [sprint.md](./sprint.md) · [tech-debt.md](./tech-debt.md) · [contributing.md](./contributing.md)

> Riwayat historis Smallholder HUB: perintah audit, Decision Log, dan Changelog per bulan.
> **Jangan gunakan file ini sebagai acuan status.** Status resmi ada di tabel **Phase Status** pada [`roadmap.md`](./roadmap.md).
> Dipisah dari `progress.md` (restrukturisasi 2026-07-12) agar file status tetap ramping.

Format: **Decision Log** dan **Changelog** dikelompokkan **per bulan** dalam section collapsible (`<details>`), bulan terbaru paling atas. Entri baru ditambahkan sebagai baris teratas tabel di dalam section bulan berjalan (buat section `<details>` baru saat ganti bulan).

### Ringkasan Dua Minggu Terakhir (02–15 Juli 2026)

> Snapshot per 2026-07-15 — perbarui/ganti section ini saat menulis ringkasan periode berikutnya.

- **Modul Peta (stream MAP):** Peta Lahan (MAP-01) selesai + enhancement — produksi asli per-lahan di popup, "Profil Lahan" PDF, panel Daftar Lahan, layer Titik Api NASA FIRMS, tool Ruler geodesik, label KT/petani, overlay referensi SIGAP KLHK & tambah GIS sendiri (WMS/Shapefile/GeoJSON). Peta BMP (MAP-02, #144): layer Ketersediaan Data Produksi 4-kategori + cetak PDF & Excel matriks.
- **Report:** Report Petani (RPT-01, #107), Report Produksi matriks bulanan (RPT-03, #132), dan Report Kelompok Tani real-time Summary + Detail (#154) — semua dengan export Excel/PDF.
- **Dashboard & Data Analyst:** Main Dashboard snapshot-backed (DASH-01, #99) + card baru "Total Kelompok Tani" (#148); **Dashboard BMP Produksi snapshot-backed (DASH-04, #166)** + tools generate snapshot + filter Kelengkapan Data (Semua ⇄ Full 1 Tahun); Analisa Ketersediaan Data Lembaga Petani (DA-02, #118) + enhancement domain Pelatihan (DA-02b, #122).
- **Audit & keamanan:** audit menyeluruh 2026-07-10 → remediasi tuntas #125–#130 (guard RBAC + scope by-id, lint 193 error → 0, konvensi UI, cleanup deps/dead code, audit fields, font brand WRI) + fix scope-leak `getMapData` (BUG-007).
- **Hierarki kelembagaan:** koreksi domain Petani → Kelompok Tani → Lembaga Petani (#146) — sub-kelompok per-lahan `subGroupLv1`/`subGroupLv2` + `blok`; relabel "Lembaga Petani" (#147/#155); mapping shapefile → Gapoktan/KT/Blok di bulk upload (#150); KT/Gapoktan turunan di detail Petani (#152).
- **Lembaga Petani (#160):** field Tipe Grup, split Tahun Berdiri vs Bergabung, Sertifikasi RSPO, kode ICS → ISH + pengisian data 31 lembaga.
- **Performa (#163):** audit 22 server action — select ramping & agregasi DB di 6 modul list (geometry keluar dari payload list Lahan/Produksi; agregasi Lembaga via `groupBy`); aturan "Payload Trimming" di `performance.md`.
- **Docs & proses:** restrukturisasi `docs/` (progress.md → roadmap/sprint/changelog/tech-debt), rule pre-commit "docs sync", rule retrospektif sebelum close issue; **standar versioning SemVer** (`standards/versioning.md`) + rilis pertama **v0.9.0 → v0.10.0 → v0.11.0**.
- **QA:** test tumbuh ~216 → **441** ✅, lint **0 error**, build ✅.

---

### Audit Commands

Audit terakhir menggunakan:

```text
find src/app -type f
find src/server src/lib src/components src/validations src/test -type f
find prisma -type f
rg "Dashboard|BMP|Training|Farmer|Parcel|Production|Staff|HCV|Coming soon" src prisma scripts docs
git ls-files | grep '\.DS_Store$'
npm test
```

### Decision Log

<details>
<summary><strong>Juli 2026</strong></summary>

| Tanggal    | Keputusan                                                                                                                      |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-22 | **#189 — Gapoktan/KUD dihapus: hierarki final 3 level (Petani → Kelompok Tani → Lembaga Petani)** — (1) Keputusan owner: level **Gapoktan/KUD dibatalkan** (sebelumnya `LandParcel.subGroupLv1`, per-lahan interim). Kolom DB **`sub_group_lv1` di-drop** (migrasi `20260722030000_drop_gapoktan_sub_group_lv1`, keputusan owner "drop langsung" tanpa cek volume). Menutup pertanyaan lama TD-013/TD-014 "3 vs 4 level" → **3 level final**. (2) **Laporan Kelompok Tani direstrukturisasi**: Summary dari (Lembaga × Gapoktan × KT) → **(Lembaga × KT)** (hapus KPI `totalGapoktan`); Detail dari hierarki **Gapoktan → KT → Petani** → **KT → Petani** (flat; `gapoktanList` → `kelompokTaniList`). Baris yang dulu beda hanya karena Gapoktan kini **melebur** ke satu baris KT. (3) **Bulk upload shapefile**: alias kolom Gapoktan di `parcel-bulk-mapping.ts` dihapus; kolom mapping/preview/Excel Gapoktan dibuang. (4) **UI**: input Gapoktan/KUD di form lahan, field detail lahan, kolom list, kartu "Kelompok Tani / Gapoktan" + sub-teks di detail Lembaga, tabel struktur, chip Gapoktan detail petani, baris popup Sebaran Lahan, dan kolom opsional di Report Lahan **semua dihapus**. (5) **Sisir menyeluruh** (~40 file): skema, validasi, tipe, actions (report/farmer/farmer-group/land-parcel), libs (report-kelompok-tani[-detail], farmer-sub-groups, farmer-[group-]detail, report-land-parcel), tes, Bantuan (istilah/tutorial/referensi/report/bulk-upload), docs. `deriveFarmerSubGroups` kehilangan `.gapoktan`; `GroupDetailSummary` kehilangan `gapoktanCount`. (6) **Blok** (`LandParcel.blok`) **tetap** — bukan bagian drop ini. Migrasi ke mis-prod: menunggu konfirmasi (irreversible DROP COLUMN). Gate lint/build/test menyusul. |
| 2026-07-22 | **#188 — popup lahan: layout + aksi Lihat Detail/Edit di 3 peta + STANDAR popup peta** — (1) Popup **Sebaran Lahan** sebelumnya memakai gaya popup **default maplibre** (kotak putih, tombol close menimpa header, `maxWidth 260` memotong nilai) → dirombak ke **struktur popup standar** yang sudah dipakai Peta Lahan/BMP. (2) **Keputusan owner: standarkan gaya popup peta jadi rule.** Primitif diekstrak ke **`src/components/shared/map-popup.tsx`** (`MapPopupHeader`/`MapPopupHighlight`/`MapPopupSection`/`MapPopupRows` + `MAP_POPUP_PROPS`); didokumentasikan di `docs/standards/ui-ux.md` §Geospatial. Latar kartu/radius/bayangan sudah global di `globals.css` — yang kurang hanya struktur isi. (3) **Footer aksi baru** `ParcelPopupActions` di **ketiga** peta: **Lihat Detail** (route `/admin/master-data/parcels/{id}`, gate `VIEW`) + **Edit Lahan** (buka `ParcelFormModal`, gate `EDIT` pada `master-data-parcels`); Peta Lahan tetap punya **Profil Lahan** (PDF) via slot `children`; Peta BMP dapat footer yang sebelumnya tak ada. (4) **Edit di-hoist** ke container peta via `ParcelEditModalHost` — data lahan + petani diambil **lazy** (`getLandParcelById` + action baru `getParcelFarmerOptions`) saat host di-mount `key={parcelId}` (bukan preload prop). **Pitfall:** `getFarmerOptions` di `@/lib/select-options` **bukan** Server Action → mengimpornya ke klien menarik Prisma/`pg`/`fs` (build gagal `Module not found: fs`); dibungkus jadi action `getParcelFarmerOptions` di `land-parcel.ts`. (5) **Refresh peta pasca-simpan** berbeda per sumber data: Sebaran Lahan (server props) cukup `router.refresh()`; Peta Lahan/BMP (fetch di klien) mengoper callback `onParcelUpdated` → refetch GeoJSON. (6) RBAC dijalarkan dari page (`hasPermission master-data-parcels VIEW/EDIT`) → client → canvas → popup; tombol hilang bila tak berhak. `id` lahan ditambah ke properties GeoJSON Sebaran Lahan (payload sudah bawa `id`). QC visual di Chrome: popup standar + Edit modal terisi (dibatalkan, tanpa tulis prod). Docs Bantuan `p-5-peta-lahan.md` diperbarui. Gate lint 0 / build / **test 675** ✅. |
| 2026-07-22 | **#187 bagian B — perombakan UI Settings (Menu Management + Role & Permission)** — (1) **Fix bug level-3:** kedua halaman kini render **rekursif** via helper bersama `src/lib/menu-tree.ts` (`buildMenuTree`/`flattenTree`/`descendantKeys`/`collapsibleKeys`), menggantikan pola datar `parents` + `getChildren` yang membuat menu level-3 tak pernah muncul di matriks Role & Permission. (Menu Management sebelumnya sudah 3 level secara manual — kini disatukan ke helper yang sama.) (2) **Collapsible per induk** di kedua halaman: default *collapsed*, state di `localStorage` via hook `useCollapseState` (kunci `role-matrix:open` / `menu-list:open`), tombol **Buka semua / Tutup semua** (nonaktif saat mencari). (3) **Role & Permission** dirombak: **header sticky** (baris role + baris C/V/E/D) **+ kolom Menu sticky** dalam scroll box (`border-separate` agar border sticky tetap tampil); **selektor role** (chip toggle, default semua, minimal 1); **search** menu (leluhur ikut tampil, subtree cocok di-expand paksa); **aksi massal** tombol `ListChecks` per baris (toggle semua izin × role tampil) + **kaskade induk→anak dengan dialog konfirmasi** (Batal · Hanya menu ini · Termasuk sub-menu); **feedback optimistis** lewat server action baru `setRolePermissions(updates[])` (satu transaksi, timeout 20s untuk kaskade besar) — mengganti `toggleRolePermission` + `router.refresh()` penuh per klik, revert + toast bila gagal. (4) **Keputusan owner:** **SUPERADMIN dikecualikan** dari matriks (selalu akses penuh & tak dapat diubah → 20 sel terkunci hanya menambah scroll; dicatat di legend "SUPERADMIN selalu akses penuh (tidak ditampilkan)"). (5) Docs katalog (`role-permission.md`, `menu-management.md`) diperbarui; +9 unit test `menu-tree.test.ts` (render 3 level, collapse, search leluhur, descendant/collapsible). Gate lint 0 / build / **test 675** ✅. Bagian A (role DONOR) sudah lebih dulu di-*commit* & diterapkan ke mis-prod. |
| 2026-07-22 | **#187 bagian A — Role `DONOR` (donor/funder read-only)** — (1) Issue #187 memuat dua bagian (A: role DONOR; B: perombakan UI Settings). Owner memilih **kerjakan A dulu** (risiko rendah, langsung dipakai); **B jadi issue/PR terpisah** — bug render **level-3 di matriks Role & Permission** (`parents` + `getChildren` datar) belum diperbaiki dan tetap masuk lingkup B. (Catatan: Menu Management sudah merender 3 level, jadi klaim "kedua halaman 2 level" pada #187 sebagian usang.) (2) **Enum `Role.DONOR`** (`_config.prisma`) + migrasi `20260722010000_add_donor_role` (`ALTER TYPE "Role" ADD VALUE 'DONOR'`). (3) **Sentralisasi daftar role** ke `src/lib/roles.ts` (satu sumber: urutan tampil + `ROLE_BADGE_CLASS` + `ROLE_DESCRIPTION`), menggantikan **5 titik hardcode** (`user.schema.ts` ×2 `z.enum`, `user-form-modal.tsx` `ROLES`+cast, `user-list-client.tsx` peta warna badge, `role-matrix-client.tsx` `ROLES`) — penambahan role berikutnya cukup satu titik. (4) **Hak akses DONOR = VIEW-only** atas **Dashboard + Report + Map (+ Bantuan)**, tanpa CREATE/EDIT/DELETE, **tanpa** Master Data/Bulk Upload/Tools/Settings/Data Analyst (15 baris di `role-permissions.csv`). (5) **Privasi (keputusan owner):** untuk sementara DONOR **boleh lihat data individu petani (nama/NIK) seperti MANAGEMENT** — pemisahan agregat-saja ditunda ke menu khusus DONOR nanti; **ekspor Excel/PDF diperbolehkan** mengikuti VIEW halaman. (6) **Data-access scope** tetap berlaku: tanpa assignment = `ALL`, dengan `UserProvince`/`UserDistrict`/`UserFarmerGroup` = terbatas. (7) Docs Bantuan (`1-3-hak-akses.md`, `a-2-mengelola-pengguna.md`) + docs status (`security.md`, `performance.md`, `module-status.md`) diperbarui 4→5 role. +3 test (DONOR VIEW-only, tolak master data, terima role di schema). Gate lint 0 / build / **test 666** ✅. **Migrasi + seed role-permission diterapkan ke `mis-prod`** (target dikonfirmasi owner): `migrate deploy` (hanya `20260722010000` pending) + upsert 15 baris DONOR via skrip parsial (dry-run dulu) — verifikasi DB: 15 baris DONOR, semua VIEW, aktif. |
| 2026-07-21 | **Koreksi: repo PUNYA CI — `workflow.md` selama ini menyatakan sebaliknya** — (1) `workflow.md` & `versioning.md` menulis "tidak ada CI otomatis", padahal ada **4 workflow** aktif: `gitleaks` (tiap push & PR, memindai seluruh riwayat), `semgrep` (SAST di PR), `deploy-dev.yaml`, dan `deploy-main.yml`. Klaim itu keliru sejak lama dan sempat membuat asumsi kerja salah selama sesi ini. (2) Yang benar: **lint/build/test memang tidak dijalankan CI** — enforcement lokal tetap sesuai keputusan owner; yang berjalan di CI adalah keamanan + deployment. Keduanya kini dibedakan eksplisit. (3) Konsekuensi yang paling perlu diingat dan sebelumnya tak tertulis di mana pun: **merge PR ke `main` = deploy produksi otomatis** lewat SSH + `pm2 reload`. Tidak ada langkah manual terpisah. (4) `deploy-main.yml` menjalankan `prisma generate`, **bukan** `migrate deploy` — migrasi DB tetap manual dan wajib diterapkan **sebelum** merge, jika tidak kode baru bertemu skema lama di produksi. (5) Checklist rilis & Quality Gates ditambah langkah memeriksa `gh pr checks`. |
| 2026-07-21 | **Bantuan = tutorial per TUGAS, dua tingkat kedalaman dari satu sumber (HELP-02)** — (1) Bantuan lama berisi **definisi** dan terorganisasi **mengikuti menu** — organisasi referensi. Pengguna datang dengan tujuan ("mau input panen bulan ini") lalu harus menebak ada di menu mana; unit organisasinya diubah jadi tugas. Struktur jadi **tiga lapis**: tutorial · konsep · referensi, dengan rute lama dipertahankan. (2) **`docs/product/pages/` TIDAK disalin ke Bantuan.** 62 berkas itu katalog untuk developer (path file, nama action, `requirePermission`, seluruh kolom tabel); memindahkannya apa adanya justru membuat Bantuan lebih sulit. Nilainya sebagai **sumber akurasi** — label tombol & kolom dikutip persis dari sana. (3) **Dua tingkat kedalaman dari SATU sumber** (baris `1.` Ringkas, baris `+` Detail) — dua berkas terpisah ditolak karena akan cepat tidak sinkron; kalibrasi kedalaman ditetapkan owner setelah dua putaran (versi awal terlalu dangkal, revisi pertama terlalu rinci). Isi Detail tetap terindeks pencarian meski disembunyikan. (4) Toggle Ringkas/Detail & lipat daftar isi memakai **checkbox + CSS tanpa JavaScript**, konsisten sifat statis #182/#183. (5) **Troubleshooting tetap inline** di tiap tutorial (keputusan owner: "sangat berguna" di tempatnya), dan **tanpa tangkapan layar** agar tak ada gambar basi saat UI berubah. (6) Tutorial di luar hak akses **ditandai, bukan disembunyikan** — tetap berguna saat pelatihan lintas peran; konsekuensinya klaim "nol query DB" pada #182 tidak lagi berlaku. (7) **Pelajaran teknis:** kelas Tailwind `peer-*` = selektor sibling, jadi hanya berlaku pada elemen bersibling setelah checkbox — dua kali membuat toggle tampak benar di kode tetapi diam di layar; CSS hasil build kini diperiksa langsung. +54 unit test termasuk penjaga kelengkapan materi (640). |
| 2026-07-21 | **Audit asimetri pembilang–penyebut di Main & BMP Dashboard — 2 celah laten, 0 dampak data** — (1) **Main Dashboard bersih**: query bertolak dari petani **aktif** (`dashboard-query.ts:47`) sehingga pembilang selalu himpunan bagian penyebut; kegiatan nonaktif pun sudah dibuang (`dashboard-aggregation.ts:113`). Tidak ada perubahan kode. (2) **BMP — rasio lahan aman**: `lahanBerData` dijaga `parcelById.has(...)` sehingga mustahil melebihi `totalLahan`. **Tetapi produktivitas (Ton/Ha) tidak simetris** — tonase ditambah untuk setiap record, luas hanya untuk lahan aktif yang dikenal → TD-022. Pemicunya `bulk-upload-parcel.ts` yang membuat baris lahan **baru** saat revisi tanpa me-*repoint* `ProductionRecord.parcelId` (`updateLandParcel` aman, update in-place). (3) **Dua definisi "cakupan pelatihan"**: Main petani-sentris, DASH-06 kegiatan-sentris → TD-023. (4) **Diverifikasi ke data, bukan diperkirakan** (skrip read-only di `scripts/local/`): 17.063 record produksi — 0 tanpa lahan, 0 menunjuk lahan nonaktif; 8.240 baris kehadiran — 0 lintas-Lembaga, 0 petani nonaktif. Kedua celah **laten**: angka dashboard saat ini benar. Tidak ada perbaikan kode yang dipaksakan karena bagian "record tanpa lahan masuk pembilang" adalah keputusan owner terdokumentasi (#136) — perlu persetujuan sebelum diubah. |
| 2026-07-21 | **Target cakupan pelatihan = 100% seluruh paket, disimpan sebagai konstanta kode** — (1) Owner memilih target **seragam 100% petani aktif untuk keempat paket** (bukan bertingkat) — setiap petani idealnya melewati seluruh paket; konsekuensinya sel matriks jarang "hijau penuh", dan itu memang gambaran yang dikehendaki. (2) Disimpan sebagai konstanta `TRAINING_COVERAGE_TARGET` di `lib/training-dashboard-aggregation.ts`, **bukan tabel konfigurasi DB** — target jarang berubah sehingga migration + halaman setting tidak sepadan; mengubah target = satu baris + deploy. `OTHER` bernilai `null` (di luar paket program, tidak dinilai kurang). (3) **Drill-down**: sel yang belum mencapai target bisa diklik → daftar petani belum dilatih, **diambil on-demand** lewat `getUntrainedFarmers` alih-alih ikut payload dashboard (nama seluruh petani tak perlu dikirim hanya untuk menghitung). Daftar memuat Nama + ID Petani + L/P dan **sengaja tanpa NIK** — peruntukannya undangan pelatihan, bukan verifikasi identitas. (4) Sel yang sudah memenuhi target dibuat **tidak bisa diklik** (bukan sekadar modal kosong) supaya keterklikan itu sendiri jadi penanda "ada pekerjaan di sini". +6 unit test target/gap (567). |
| 2026-07-21 | **DASH-06 Dashboard Pelatihan = live query, bukan snapshot-backed** — (1) Pertanyaan owner "dashboard pelatihan bagusnya seperti apa": dashboard dibedakan perannya dari **Report Pelatihan** (`report-training`, wajib Distrik+Lembaga, orientasi cetak per kegiatan) — dashboard menjawab **"program sudah sejauh mana, Lembaga/paket mana yang tertinggal"**: org-wide, tanpa filter wajib, eksploratif. (2) **Tidak dibuatkan tabel snapshot** (kontras DASH-01/DASH-04): volume pelatihan kecil (ratusan kegiatan) sehingga `getTrainingDashboardView()` query langsung + agregasi client-side dari satu payload per-Lembaga; menghindari beban operasional "generate snapshot dulu" dan angka basi — konsekuensinya bila volume tumbuh besar, pola snapshot tetap tersedia sebagai jalur migrasi. (3) **Denominator cakupan = seluruh petani aktif Lembaga** (keputusan owner, bukan hanya petani di Lembaga yang pernah dilatih) supaya sisa pekerjaan terlihat jujur; Lembaga yang belum tersentuh tetap masuk pembagi dan ditandai merah di matriks. (4) **Filter Paket sengaja tidak dipasang** di header — matriks, chart, dan tabel skor sudah memecah per paket, filter global hanya menduplikasi sumbu yang sama; default Tahun = **Semua Tahun** karena cakupan bersifat kumulatif. (5) Skor post < pre ditandai eksplisit sebagai **indikasi salah input**, bukan dibaca sebagai hasil belajar menurun. +26 unit test termasuk RBAC scope where-fragment 3 mode (561). |
| 2026-07-20 | **#185 Media Bantuan dari S3 = sintaks `s3://key`, presign saat render** — (1) Pertanyaan owner "gambar/video bisa dari S3?": bisa, tapi **bucket proyek privat** (`lib/s3.ts` presigned, pola bukti pelatihan) sehingga URL bertanda tangan **tak boleh ditulis mati** di Markdown (kedaluwarsa). (2) Sintaks `![caption](s3://help/file.mp4)` + resolver server `lib/help-media.ts` mengganti tiap `s3://key` dengan presigned URL **per-request** di halaman topik → tautan selalu segar; sumber lain (path `public/help/…`, URL publik, YouTube/Vimeo) dilewatkan apa adanya. (3) **Gagal presign tidak mematikan halaman**: blok media dibuang, materi lain tetap tampil, kesalahan masuk log server. (4) Panduan pemilihan sumber didokumentasikan (`src/content/help/README.md` + `public/help/README.md`): repo untuk gambar kecil, **S3 untuk video/aset besar** (tak membebani repo, ganti isi tanpa deploy ulang), YouTube untuk video publik panjang. +4 unit test ber-mock presign (535). |
| 2026-07-20 | **#184 Bantuan: sub-halaman per topik + pencarian + konten pindah ke Markdown** — (1) **Konten keluar dari kode**: 11 topik jadi file `src/content/help/**.md` ber-frontmatter (`title`/`icon`/`intro`), di-bundle webpack **`asset/source`** (pola `.csv` existing — tanpa baca filesystem saat runtime) → materi bisa diedit lewat GitHub tanpa menyentuh JSX, riwayat revisi dari git; `src/content/help/README.md` mendokumentasikan subset sintaks. (2) **Parser Markdown subset sendiri, tanpa dependency baru** (`lib/markdown-lite.ts`): frontmatter, `## heading`, paragraf, list, baris definisi `**Istilah** — penjelasan`, inline tebal/kode/tautan, plus **media** `![caption](src)` (gambar · `.mp4`/`.webm` → `<video>` · YouTube/Vimeo → sematan `youtube-nocookie`) — dirender ke **elemen React**, jadi tak ada jalur `dangerouslySetInnerHTML`; alasan tanpa lib: konten berasal dari repo (tepercaya) & subsetnya kita kendalikan. (3) **Rute 3 tingkat** (revisi owner saat QC): indeks → ikhtisar bab → **satu topik = satu halaman** `[chapter]/[topic]` (topik akan berisi langkah/tutorial detail) + tombol topik sebelumnya/berikutnya **lintas bab**; `generateStaticParams` untuk kedua rute, slug tak dikenal → `notFound()`. (4) **Pencarian client-side** atas indeks ringan (bab+judul+isi, cocokkan semua kata) — satu-satunya bagian ber-`"use client"`; sisanya tetap Server Component. Gate lint 0 / build / test **531** (+12). |
| 2026-07-20 | **#183 Bantuan = tree view per bab, tetap tanpa JavaScript** — (1) 11 section datar #182 direstrukturisasi jadi **6 bab → 11 topik** (Memulai · Mengelola Data · Memantau & Menganalisa · Laporan & Cetak · Administrasi · Bantuan Lanjutan); **isi materi tidak berubah**, hanya pengelompokan + penomoran **`bab.topik`** (mis. "3.2 Peta") agar bisa dirujuk saat pendampingan. (2) **Tree view pakai `<details>` native** (bukan komponen accordion ber-state) → halaman tetap **Server Component tanpa `"use client"`**, konsisten sifat statis #182 dan pola collapsible di `docs/`. (3) Nav kiri **sticky** di `lg+`, stack di layar kecil; konten dikelompokkan per bab (header bab ber-ringkasan → kartu topik ber-anchor `scroll-mt`). (4) Struktur data `CHAPTERS[] → topics[] → items[]` — menambah bab/topik = mengedit data, bukan markup. |
| 2026-07-20 | **#182 Menu Bantuan = halaman panduan STATIS (Server Component), bukan file HTML mentah** — (1) Owner minta "static html cara menggunakan MIS"; dipilih **Server Component tanpa data** (`/admin/help`) karena file `.html` mentah tak ikut routing/layout/sidebar Next.js & tak bisa di-guard RBAC — sifat "statis" tetap terjaga (nol query DB, nol server action, tanpa `"use client"`; konten sebagai konstanta ber-tipe). (2) Menu **top-level `help`** order 9 + **VIEW untuk 4 role** (panduan berlaku semua peran) + guard `requirePermission("help")` demi konsistensi RBAC lapis 1. (3) **11 section ber-anchor + daftar isi**, ditulis untuk pengguna non-teknis: istilah domain (Petani→KT→Gapoktan/KUD→**Lembaga Petani**, KT melekat pada LAHAN), soft delete "nonaktif bukan hapus", revisi lahan, sensor NIK di layar vs export utuh, **kenapa angka dashboard tak langsung berubah** (snapshot → generate di Tools), grid peta & label Laporan Lahan, FAQ kendala umum. (4) Konten diturunkan dari `docs/product/*` — **wajib ikut diperbarui saat alur modul berubah** (risiko drift dicatat). Seed parsial ke DB (dry-run dulu) ✅. |
| 2026-07-20 | **#180 TD-018 + TD-019 + polish peta cetak RPT-05** — (1) **TD-018 ✅**: 10 action dropdown Distrik/Lembaga di `report.ts` → delegasi 2 helper privat `districtsForMenus`/`farmerGroupsForMenus` (API & permission key per-menu tak berubah; KT family = menuKeys ganda). (2) **TD-019 ✅**: `pdf.ts`/`farm-passport.ts`/`bmp-map-print.ts` dipisah **build-vs-save** (pola #179) + 5 test struktural `pdf-exporters.test.ts` (orientasi/halaman/tanpa-throw — akar bug print #174/#179 kini ter-guard test). (3) **Polish peta cetak**: **anti-tumpang label** (`resolveLabelCollisions` greedy geser vertikal ber-clamp, dipakai jsPDF & SVG), **skala batang** (`pickScaleBar` jarak nice 100 m–20 km dari skala proyeksi, valid lintang rendah) + **panah utara "U"**, **mini-ikhtisar posisi sel** di tiap halaman peta sel (PDF + preview). Gate lint 0 / build / test **519** ✅ (+9). |
| 2026-07-20 | **Fix label vertikal PDF (QC owner: teks melenceng keluar pill) + Excel multi-sheet ber-gambar peta + ceklis label +KT** — (1) **Akar masalah**: jsPDF menghitung offset `align`/`baseline` **pra-rotasi** saat `angle` dipakai → teks vertikal bergeser sejauh ~setengah lebar teks (teks panjang meleset paling jauh, pill kosong); fix: anchor manual `verticalLabelAnchors` (pure — baris ditumpuk per kolom, baseline 75% kolom karena ascent menghadap kiri, center vertikal) **diverifikasi empiris dari content stream PDF** (matrix Tm 90° CCW, semua baris di dalam pill; script `scripts/local/verify-vertical-label.mjs`). (2) **Excel ber-gambar peta**: sheet "Lahan" penuh + **gambar peta index** di kanan tabel; grid aktif → **satu sheet "Peta A1…" per sel** berisi subset baris sel + gambar peta selnya — SVG preview (komponen yang sama) di-rasterisasi ke PNG via canvas (`svgToPng`), workbook builder (`buildLandParcelWorkbook`, build-vs-download) teruji exceljs. (3) Ceklis Label Poligon tambah opsi **Kelompok Tani**. Test 505→**509**. |
| 2026-07-20 | **#179 Laporan Lahan revisi owner pasca-QC: peta ber-grid index di PDF + preview + Lembaga wajib** — (1) **Lembaga Petani wajib** (laporan & cetakan selalu per 1 Lembaga; field Cari dihapus — halaman ini alat cetak per Lembaga, card "Lembaga Petani" dihapus). (2) **PDF landscape ber-halaman peta**: poligon lahan digambar **vektor jsPDF** (generalisasi `drawPolygon` farm-passport ke multi-poligon dalam bounds bersama, utara di atas), lahan tanpa geometri tetap di tabel + dicatat di bawah peta; geometry di-fetch **per-Lembaga on-demand** (`getLandParcelReportGeometries`) agar payload list tetap tanpa geometry (#163). (3) **Grid index (atlas)** — jawaban QC owner "700 label jadi bubur": pilihan pecah **1/4/9/16**; halaman ikhtisar ber-garis grid + label sel (A1, A2, … huruf=baris dari utara) + jumlah lahan per sel, lalu **1 halaman per sel berisi** (lahan masuk sel per **centroid** → tak pernah terpotong karena tiap halaman sel me-refit bounds; sel kosong dilewati). (4) **Ceklis Label Poligon** (No/Nama/ID Petani/ID Lahan, min. 1): No-saja = lingkaran kecil, multi-isi = pill multi-baris. (5) **Preview on-page (SVG)** langsung di bawah pengaturan grid+label — memakai **helper layout yang sama** dengan PDF (`buildLandParcelMapLayout`/`splitParcelsIntoGrid`) sehingga preview = hasil cetak. (6) Kolom baru: **Tahun Tanam + Luas default tampil** (revisi owner), Komoditas/Species/PSR opsional. (7) Test 485→**499** (+14: layout/grid/**verifikasi empiris jsPDF** — build dipisah dari save agar dokumen bisa diperiksa; pelajaran #174). (8) **Revisi lanjutan owner**: label poligon **adaptif** (`fitLabelToBox` pure, dipakai jsPDF & SVG agar identik: horizontal → putar 90° bila lebih lega → skala turun lantai 0.55) + grid index **fleksibel Baris × Kolom** (input bebas mis. 2×3, baris maks 26 = label A–Z; menggantikan pilihan persegi 1/4/9/16). Test → **505**. |
| 2026-07-20 | **Field lahan baru `species` + `isPsr` (PSR) + data fix Komoditas** — (1) Migrasi `20260720044357`: `species` (String?, species komoditas mis. "Elaeis guineensis") + `is_psr` (Boolean default false; **PSR = Peremajaan Sawit Rakyat** — lahan sedang replanting, produksi 0 wajar). `cropType` existing = **Komoditas**. (2) Master data Lahan: form (input Species + checkbox PSR), list (kolom Species + badge PSR), detail, Excel. (3) **Data fix: 4.163 lahan (semua) di-set Komoditas "Kelapa Sawit"** via `scripts/local/update-crop-type-kelapa-sawit.mjs` (dry-run dulu; **gotcha Prisma: `NOT {equals}` tak mencakup `null`** — kondisi harus `OR [null, NOT]`; tertangkap justru karena dry-run menunjukkan target 0). Species dibiarkan kosong menunggu data. (4) Catatan #178: lahan `isPsr=true` harus **dikecualikan/dipisah** dari kategori "Produktivitas Rendah" pada Deteksi Anomali. |
| 2026-07-20 | **#177 Report Lahan (RPT-05) = roster datar real-time, kolom sesuai permintaan + ekstra via column selector** — (1) Submenu `report-land-parcel` ("Lahan", order 6): grain **1 baris = 1 lahan aktif** dengan 5 kolom sesuai #177 (Lembaga Petani | Nama Petani | ID Petani | ID Lahan | Kelompok Tani); kolom ekstra **Gapoktan/KUD, Blok, Luas (Ha)** default tersembunyi via column selector (pola RPT-04), footer total luas hanya saat kolom Luas tampil. (2) **KT = `LandParcel.subGroupLv2` per-lahan** (konsisten #146/#152 — bukan atribut petani), normalisasi trim (kosong → "-") + distinct KT summary case-insensitive per Lembaga (pola #154). (3) **Real-time bukan snapshot** (data ringan, pola RPT-04); filter Distrik/Lembaga **opsional** (default semua sesuai scope user), cetak per-Lembaga via filter + metadata PDF Distrik/Lembaga. (4) RBAC 3 lapis + **select ramping tanpa `geometry`** (#163); read-only, tanpa migration — hanya seed menu + 4 role-permission VIEW. (5) Pure lib `report-land-parcel.ts` + 5 unit test (urutan Lembaga→KT→Petani→ID Lahan, null di akhir). Gate lint 0 / build / **test 485** ✅. Catatan operasional: seed menu+permission perlu diterapkan ke DB (jangan full seed — pola #148). |
| 2026-07-17 | **#174 Peta BMP Layer 2 Produktivitas = realtime client-side (bukan snapshot) + panel 2 section layer** — (1) **Realtime tanpa perubahan server/tabel** (keputusan owner atas opsi snapshot-backed): `getBmpMapData` sudah memuat `area` + produksi per persil per bulan dalam 2 query bounded per Lembaga — Ton/Ha dihitung pure helper client-side (`buildBmpProductivityView` dkk. di `map-data.ts`), zero query tambahan, ganti tahun/layer instan; snapshot justru menambah beban (tabel+tools+data basi) tanpa manfaat. (2) **Panel kiri = 2 section layer ber-radio** ('Ketersediaan Data Produksi' / 'Produktivitas (Ton/Ha)'; judul langsung tanpa prefix Layer 1/2 — revisi owner), satu aktif pada satu waktu karena mewarnai poligon yang sama (revisi owner atas usulan mode-switch dalam satu section); tombol Cetak/Excel ke area bersama bawah panel. (3) **Selektor Tahun default tahun terbaru + Rata-rata** (rata-rata Ton/Ha antar tahun melapor, konfirmasi owner); nilai **apa adanya** (tidak disetahunkan) dengan indikator 'Bulan Melapor n/12' di popup; badge popup **kelas saja** (nilai di baris Detail Lahan — revisi owner). (4) **Ambang kelas 10/15/20 Ton/Ha** (usulan #174, konstanta `PRODUCTIVITY_*_MIN` mudah direvisi); Tidak Ada Data = outline saja (area null/0 atau tanpa data view; produksi tanpa `parcelId` tak dihitung, pola #136). (5) **Cetak & Excel = WYSIWYG ikut layer aktif** (keputusan owner): layer Produktivitas mencetak legend produktivitas + **tabel produktivitas per lahan** (Ton/Ha per tahun + Rata-rata, sel diwarnai kelas) menggantikan matriks ketersediaan, Excel sheet Produktivitas, label tombol + nama file ikut layer; cetak 2 layer sekaligus ditolak (capture 2× rapuh). (6) **Review pass 8-finder/10-verifier: 10 temuan CONFIRMED diperbaiki** — legend PDF wrap multi-baris (overflow 325mm→280mm, diverifikasi jsPDF), label kelas cp1252-safe ("min. 20" — "≥" garble di font core jsPDF), popup refresh saat ganti view (anti-basi), sanity window tahun produksi (typo "2924" tak jadi view default), metadata kelas dipusatkan `BMP_PRODUCTIVITY_CLASSES` di `map-data.ts` (label ber-ambang derive dari konstanta; tint PDF derive dari hex yang sama), legend PDF ketersediaan ikut `BMP_CATEGORIES` (drift "thn"/"tahun" hilang), diskriminator `productivityIsAvg` terstruktur, fix 4 error `tsc --noEmit` fixture, gating rebuild GeoJSON per mode + memo JSON produksi. Gate lint 0 / build / **test 480** ✅ (+15 unit, +1 perf: view+matrix 500 lahan 18,9ms). |
| 2026-07-17 | **Import data pelatihan 4 ICS via script lokal + koreksi "PNC & NKT = satu kegiatan" + relabel MK "Manajemen Kelompok"** — (1) Import Excel fasilitator → `TrainingActivity`/`TrainingParticipant` untuk **KP KBM (74 act/2.454 part), APKSSB (31/1.147), KUD Karya Sembada/KSM (15/883)** + revisi FORTASKI (50→**39/946**) via `scripts/local/import-training-*.mjs` (pola FORTASKI: idempotent, dry-run default, laporan `-tidak-terimport.xlsx` + file `-review.xlsx` ber-status YA/YA\*/TIDAK per baris untuk fasilitator). (2) **Koreksi owner: modul P&C (PNC) & NKT adalah SATU kegiatan** — sheet-nya duplikat; digabung 1 activity ber-location "Modul PNC & NKT" (import lama PNC/NKT terpisah di-revert & re-import; sesi "NKT 31 Mei 2025" FORTASKI dihapus mengikuti file sumber revisi — skor identik dgn sesi 16 Mei). (3) **Relabel `PAKET_2_MK`: "Manajemen Kebun" → "Manajemen Kelompok"** (baris DB + label UI list/report + seed CSV + docs). (4) Guard kualitas data: parser tanggal teks Indonesia (bulan penuh/singkat, pemisah spasi/strip, typo "Okrober"/"Ju;i"), **sanity tahun 2015–2026** (menangkap typo "2924" di APKSSB — 1 participant dipindah manual ke activity 2024-08-14), skor desimal dibulatkan (kolom Int); **tanggal kosong = petani belum ikut training** → hanya masuk laporan, bukan error. Total training di DB: **159 activity, 5.430 participant** (4 ICS). |
| 2026-07-16 | **#172 Detail Petani = profil 360° mengikuti pola #171, satu fase penuh** — (1) tabs Ringkasan/Lahan/Pelatihan/Produksi + **5 summary cards** (Lahan+Luas · Produksi · Pelatihan n/paket · **Kelengkapan Profil** · **Produktivitas tahun terakhir** — pilihan owner "dua-duanya"); (2) **PDF "Profil Lahan" (#134) masuk scope** (tombol per lahan, fetch via action ber-guard menu petani); (3) **foto petani pending → TD-017**, detail pakai placeholder avatar inisial; (4) komponen peta sebaran #171 **diangkat ke shared** + logika produksi per-tahun/bulanan diekstrak agar tidak duplikat. |
| 2026-07-16 | **#171 Detail Lembaga Petani = profil 360° ber-Tabs, Fase 1 dulu** — (1) struktur **Tabs** (header + 5 summary cards selalu tampak; tabs Ringkasan/Petani/Lahan/Pelatihan/Produksi) dipilih owner atas section bertumpuk; (2) **Fase 2 pending** (tab Petani & Lahan mendalam: pertumbuhan keanggotaan, distribusi umur tanam, pre/post test) — data belum memadai; Fase 1 = tab Ringkasan+Pelatihan+Produksi penuh, Petani/Lahan versi ringkas; (3) skor kelengkapan DA-02 = **card + link** (bukan embed — hindari duplikasi halaman DA-02); (4) panel akses pengguna (UserFarmerGroup) **di-skip**; (5) **real-time** bukan snapshot (konsisten keputusan #153/#154: detail 1 entitas); action tunggal `getFarmerGroupDetail` ber-RBAC 3 lapis — **tidak** memanggil action report (beda permission menu), reuse pure lib #154/MAP-02/DA-02 langsung. |
| 2026-07-16 | **Aturan rilis: maksimal satu rilis per hari** — beberapa pemicu dalam sehari (beberapa phase Done / `feat:`) digabung jadi satu rilis di akhir hari dengan bump tertinggi; pengecualian hanya hotfix kritis produksi. Lahir dari 2026-07-15 yang menghasilkan 3 rilis beruntun (v0.9.0 → v0.10.0 → v0.11.0) dalam sehari. Diformalkan di `standards/versioning.md` (Titik rilis + checklist). |
| 2026-07-16 | **Sertifikasi ISPO & Assurance SAP/MAP (#169) = pola RSPO #160 + enum generik `CertStatus`** — (1) tipe data mengikuti keputusan #160: Int+enum per skema (bukan string bebas), status boleh tanpa tahun, tahun tanpa status ditolak Zod; (2) **enum generik baru `CertStatus`** (CERTIFIED/PLANNED) untuk ISPO & SAP/MAP — reuse `RspoCertStatus` ditolak (nama misleading), rename enum existing ditolak (migrasi tipe Postgres berisiko), `RspoCertStatus` dibiarkan; (3) **SAP/MAP satu pasangan field** dengan label gabungan "Assurance SAP/MAP" (konfirmasi owner; pola label "Gapoktan/KUD"); (4) **card sertifikasi Main Dashboard menyimpan status per Lembaga di `KTDetails`** (bukan angka global) agar rekap `certStats` dihitung ulang saat scoping/slicing viewer — konsisten pola #148; (5) formatter status+tahun disatukan `formatCertStatus` (RSPO/ISPO/SAP-MAP satu logika); (6) data sumber WRI/Unilever belum punya kolom ISPO/SAP-MAP (konfirmasi owner) → kolom kosong dulu, tanpa data fix. |
| 2026-07-15 | **Dashboard BMP (DASH-04, #166) = snapshot-backed + konten revisi dari mockup** — (1) **snapshot-backed** seperti Main Dashboard (keputusan owner; tabel `tbl_snapshot_bmp_dashboard` + tools generate sendiri), grain JSON **per Lembaga** (`monthly` + `byYear`) agar filter Distrik/Lembaga/Kategori/Tahun di-slice client-side dari satu snapshot org-wide; (2) **konten direvisi dari mockup awal** (pilihan owner atas usulan kritik): cards fokus produksi (tanpa duplikasi Main Dashboard), garis chart = **cakupan pelaporan (% lahan melapor)** alih-alih produktivitas (garis produktivitas ≈ bayangan bar), panel kanan = **Ketersediaan Data Produksi** 4 kategori reuse MAP-02 alih-alih Monev BMP placeholder (data monev belum ada → follow-up); (3) **filter Tahun jadi filter GLOBAL di header dengan default "Rataan"** (revisi owner saat implementasi — memfilter cards + chart sekaligus, bukan filter level chart; Rataan = cards rata-rata per tahun [Σ nilai tahunan ÷ jml tahun ber-data] + chart rata-rata bulanan; kumulatif hanya di tools detail); (4) **Produktivitas = Ton/Ha PER TAHUN**: Σ produksi(tahun terpilih) ÷ Σ luas lahan **melapor**(tahun terpilih) — bukan seluruh lahan aktif (understate saat data belum lengkap) dan bukan kumulatif lintas tahun (membesar seiring data bertambah); mode Rataan = rata-rata tahunan tertimbang luas; (5) menu di-rename **"BMP Dashboard (Produksi)"**. |
| 2026-07-15 | **Standar versioning & release resmi (SemVer) + baseline `v0.9.0`** — aturan baru di `standards/versioning.md`: SemVer `vX.Y.Z` untuk aplikasi (breaking = sisi pengguna/operasional, bukan API), tag annotated hanya di `main`, GitHub Release dengan notes dari changelog (bukan auto-generate commit). Kriteria bump: MAJOR = breaking/go-live (`1.0.0` disimpan untuk produksi; selama `0.x` breaking cukup MINOR), MINOR = phase roadmap Done/fitur baru (`feat:`), PATCH = `fix:`/`perf:`; `docs:`/`chore:` tidak memicu rilis. Titik rilis: per phase Done atau ringkasan dua mingguan. **Versi awal dipilih `v0.9.0`** (bukan lanjut `0.2.0`) — mencerminkan kematangan aktual (mayoritas PLATFORM/MD/DASH Done, MAP-01/02 selesai): posisi "MVP menjelang 1.0". Tag lama `v1.8-complete` (pra-skema, April 2026) diabaikan, tidak dihapus. `package.json` `0.1.0` → `0.9.0`. |
| 2026-07-15 | **#163 selesai: perf load list full app — select ramping & agregasi DB (5 Master Data + Region tree)** — audit full app (22 server actions): list action memakai `include` full-row tanpa `select` → payload DB→RSC→browser bengkak; **Lahan & Produksi ikut mengirim `LandParcel.geometry`** (GeoJSON, puluhan–ratusan KB/lahan) yang tak dirender list — di Produksi polygon yang sama terkirim berulang per record panen. **Fix:** `select` eksplisit sesuai interface list client di `getLandParcels`/`getProductionRecords`/`getFarmers`/`getTrainingActivities`/`getRegionTree`; `getFarmerGroups` ganti agregasi include-bersarang (1 baris per petani+lahan) → `farmer.findMany` ringan + `landParcel.groupBy(farmerId, _count+_sum area)` + merge di JS (array mentah tak lagi terkirim ke client). **Guard round-trip form:** form edit Lahan berhenti mengirim `geometry`; `updateLandParcel` kini `undefined` = pertahankan polygon existing (semula ter-null — bug laten bila geometry hilang dari payload); `evidenceKey/Name` pelatihan sengaja tetap di select (di-round-trip form edit). Tipe `LandParcel.geometry` jadi optional (diisi hanya fetch detail); prop `ParcelMapView` menerima null/undefined. Aturan "Payload Trimming" didokumentasikan di `performance.md`. +4 unit test merge agregasi (`farmer-group.test.ts` baru). Gate lint 0 / build / **test 417** ✅ (dari 413). |
| 2026-07-15 | **#152 selesai: Master Data Petani — KT/Gapoktan turunan (dari lahan) di detail Petani** — sesuai prinsip #146 (keanggotaan sub-kelompok per-lahan), petani **tidak** mendapat field KT sendiri; detail Petani kini menampilkan **Gapoktan/KUD** & **Kelompok Tani** turunan = distinct `subGroupLv1`/`subGroupLv2` dari **lahan aktif** petani (badge, multi-nilai, "—" bila kosong). Derivasi di pure lib baru `src/lib/farmer-sub-groups.ts` (`deriveFarmerSubGroups`; normalisasi trim+case-insensitive konsisten Report KT #154, label = varian pertama trimmed, urut alfabetis; **+6 unit +1 perf** [10k lahan], test 405→**413** bersama +1 perf sort/format RSPO #160). `getFarmerById` include `landParcels` aktif (select 2 field — 1 query, tanpa N+1). **Kolom list Petani (opsional di issue) sengaja di-skip**: payload list membengkak (include lahan semua petani) untuk kolom default-hidden — bila dibutuhkan, jadikan follow-up dgn agregasi di query utama. Saat KT jadi tabel (TD-014), derivasi teks → relasi. Gate lint 0 / build / **test 411** ✅. |
| 2026-07-15 | **#160 selesai: Lembaga Petani — Tipe Grup, split Tahun, Sertifikasi RSPO, kode ICS → ISH** — 4 kolom nullable baru di `FarmerGroup` (migrasi `20260715040235`): `groupType` (enum `FarmerGroupType` ASOSIASI/KOPERASI — **terpisah** dari `category` Ex Plasma/Swadaya), `establishedYear` ("Tahun Berdiri Lembaga"; `joinYear` di-relabel "Tahun Bergabung Program"), `rspoCertYear` + `rspoCertStatus` (enum `RspoCertStatus` CERTIFIED/PLANNED). **Keputusan tipe data RSPO:** bukan string bebas ("plan 2026") tapi Int+enum agar bisa difilter/direkap; render "2020" / "Plan 2026" via helper baru `src/lib/farmer-group-labels.ts` (dipakai list+detail). **Relaksasi (data riil):** tabel sumber WRI/Unilever punya "Y" tanpa tahun → status **boleh tanpa tahun** (render "Tersertifikasi"/"Plan"); kebalikannya tetap ditolak Zod (tahun tanpa status = ambigu). **Data fix ICS → ISH:** `code` 31 lembaga di DB + seed CSV + label Farmer Summary; **PK `id` sengaja tetap** berpola `ICS-` (ganti PK berisiko ke semua FK); fixture test dibiarkan (kosmetik). **Data lembaga diisi** dari tabel "Details of Smallholder Groups" (31/31 match by `code`; PPKSSM = APKSMB per konfirmasi owner; nama/abrv DB tidak diubah; Registered Farmer tidak disimpan — derived). **DataTable:** properti kolom baru `sortValue` (accessor sort kustom, backward-compatible) — sort kolom RSPO semula no-op karena `row[rspoCertYear]` null utk certified-tanpa-tahun; fix juga key export Excel (`rspoCert` → `rspoCertYear`, semula kolom Excel kosong). UI list: Tipe Grup sebelum Kategori, Total Persil default hidden. Gate lint 0 / build / **test 405** ✅. |
| 2026-07-15 | **#150 selesai: bulk upload Lahan — mapping atribut shapefile → Gapoktan/KUD/Kelompok Tani/Blok** — 3 field opsional baru (`subGroupLv1`/`subGroupLv2`/`blok`) di dynamic column mapping bulk upload Lahan (Shapefile ZIP). Logika auto-match + normalisasi **dipisah ke pure lib `src/lib/parcel-bulk-mapping.ts`** (`PARCEL_AUTO_MATCH_RULES` alias gapoktan/kud/poktan/kt/nama_kt/blok/blk/dll; `autoMatchColumns` case-insensitive+trim; `normalizeAttr` trim → kosong = null, meredam typo/inflasi distinct KT per analisa #148) agar teruji langsung (+7 unit, test 398→**405**). Preview tabel + download Excel + payload save menyertakan 3 kolom; semua **nullable** (tak memblokir impor); field ikut **revision-tracking** (insert revisi baru membawa `...record`). Backend `bulkCreateLandParcels` & `landParcelSchema` tak berubah (sudah mendukung sejak #146). Melengkapi jalur data untuk Report KT #154 & card dashboard #148. Gate lint 0 / build / **test 405** ✅. |
| 2026-07-14 | **#148 selesai: card "Total Kelompok Tani" (distinct `subGroupLv2`) di Main Dashboard + snapshot generate = Semua Data** — kartu **baru** (bukan field lama `totalKelompokTani` yang = jumlah `FarmerGroup`/Lembaga Petani, mislabel legacy). Agregasi **snapshot-backed**: distinct `subGroupLv2` **per Lembaga** (`KTDetails.kelompokTaniCount`, ternormalisasi trim/case, null diabaikan, **year-independent**) → global `stats.totalKelompokTaniLahan` = penjumlahannya; `sumKelompokTaniStats`/`scopeSnapshotData` recompute saat slice scope RBAC; `normalizeSnapshotData` default 0 untuk snapshot lama; `dashboard-query.ts` select `subGroupLv2`. UI: kartu di `summary-cards.tsx` + kolom "Total Kelompok Tani" di tabel daftar snapshot + kolom "Kelompok Tani" di detail per-Lembaga. **Semantik distinct = per-(Lembaga×KT)** (beda dgn Report #154 yg per-(Lembaga×Gapoktan×KT) — by design). **Permintaan owner:** filter generate Distrik/Tahun **dinonaktifkan sementara** (`FILTERS_ENABLED=false`) → snapshot selalu **Semua Data**; kolom Distrik/Tahun di list **default hidden**. Kartu **0 sampai** data `subGroupLv2` terisi (#150) + snapshot baru di-generate. +2 unit. Gate lint 0 / build / **test 398** ✅. |
| 2026-07-14 | **#154 selesai: Report Kelompok Tani (real-time) — 2 submenu Summary + Detail** — **Summary** (`/admin/report/kelompok-tani`): agregat real-time 1 baris per (Lembaga×Gapoktan/KUD×KT) — distinct petani, jumlah lahan, **total luas**; filter Distrik/Lembaga opsional + search + **column selector** (sembunyikan kolom, mis. Gapoktan/KUD utk Lembaga tanpa level itu) + 6 summary card. **Detail** (`/admin/report/kelompok-tani-detail`): roster per **1 Lembaga** — hierarki Gapoktan/KUD → KT → daftar Petani (jml lahan/luas), **section collapsible** (default tertutup) + Buka/Tutup semua, **auto-hide** layer Gapoktan/KUD bila Lembaga tak punya sama sekali (langsung KT→Petani). Keduanya **Excel + PDF**. Backend: pure `lib/report-kelompok-tani.ts` (+7 unit, +luas) & `lib/report-kelompok-tani-detail.ts` (+7 unit); `getKelompokTaniReport`/`getKelompokTaniDetailReport` (RBAC 3-layer, scope `farmerRelationAccessFilter` via `AND`); guard dropdown Distrik/Lembaga direlax utk melayani kedua report. **Relabel "Gapoktan" → "Gapoktan/KUD"** (Report + Master Lahan; identifier `subGroupLv1` tetap). Menu `report-kelompok-tani`(Summary)+`report-kelompok-tani-detail` + 8 role-permission (seed terarah DB, ikon `Network`/`ClipboardList`). Read-only, tanpa tabel/migration baru. Gate lint 0 / build / **test 398** ✅. |
| 2026-07-14 | **Rule baru: retrospektif wajib sebelum close issue** — sebelum menutup GitHub issue: (1) recheck, (2) tulis comment retrospektif **compact + collapsible** 6 bagian (Insight / How to solve / Challenge / Perubahan dari issue dasar / What we learned / Feedback & improvement). Ditambahkan ke `standards/workflow.md` (Issue Workflow §7). Contoh: penutup #146/#147/#149/#155. |
| 2026-07-14 | **Relabel "Lembaga Tani" → "Lembaga Petani" (revisi manajemen, #155)** — "Tani" adalah kata kerja; noun yang benar "Petani". **Cakupan: hanya "Lembaga Tani"** → "Lembaga Petani"; **"Kelompok Tani" TETAP** (istilah resmi Poktan/Permentan, = `subGroupLv2`). Sweep app (168 label `src/**` + `menu.csv` + 1 baris menu DB `master-data-groups`) + docs (59). Identifier `FarmerGroup`/`farmerGroup` **tetap** (konvensi, seperti #147). Menggantikan istilah hasil #147. Gate lint 0 / build ✅ / test 388 ✅. |
| 2026-07-14 | **Revisi: view agregat Kelompok Tani = Report real-time (bukan Master Data / bukan snapshot table)** — mengganti keputusan snapshot-backed (Opsi A) sebelumnya untuk view **tabular** KT. Owner memilih: (1) taruh di **Report** (read-only+agregat+export), bukan Master Data (CRUD). (2) **Real-time** (bukan snapshot dedicated) karena agregasi murah (perf 8ms/50k lahan #153) & pola Report existing real-time → **tabel `tbl_snapshot_kelompok_tani` dibatalkan**. Konsekuensi: **Report Kelompok Tani #154** (`getKelompokTaniReport`, grain Lembaga×Gapoktan×KT + Total Petani/Lahan); **#153 di-close (superseded)**. **#148** (card dashboard Total KT) **tetap** dari dashboard snapshot (dashboard snapshot-backed by design — hanya angka count). |
| 2026-07-14 | **"Docs sync" jadi Pre-Commit Gate ke-4 (wajib)** — sebelum setiap commit, file `docs/` yang terdampak wajib diperbarui & di-commit **bersama** kode (bukan ditunda), di samping lint/build/test. Ditambahkan ke `standards/workflow.md` dengan peta cepat (skema→`database/*`; modul/status→`project/roadmap|sprint|changelog`; aturan/arsitektur→`standards/*`+Decision Log; debt→`tech-debt.md`). +perf test **#153** (agregasi per-Lembaga distinct, 100 Lembaga × 50k lahan = 8.03ms <30ms). Test 381→**382**. |
| 2026-07-14 | **Agregat turunan land-parcel untuk Master Lembaga Petani = snapshot-backed (Opsi A)** — jumlah **Gapoktan / Kelompok Tani / Blok** per Lembaga (distinct dari `LandParcel.subGroupLv1`/`subGroupLv2`/`blok`, #146) dibaca dari **snapshot yang di-generate lewat Tools**, **bukan real-time** — mengikuti pola DASH-01 (`MainDashboardSnapshot`). **Opsi A dipilih:** perluas `MainDashboardSnapshot`/`DashboardSnapshotData` (satu generator, satu sumber) daripada tabel snapshot baru. Alasan: distinct lintas semua lahan mahal/rawan N+1 real-time; snapshot murah dibaca; stale-until-regen diterima (sama dashboard). **Batas:** hanya agregat massal (list/summary #148/#153); **detail 1 entitas tetap real-time** (#152). Dicatat di `dashboard-snapshots.md`; #148 sudah selaras (baca snapshot). |
| 2026-07-14 | **Koreksi hierarki kelembagaan + sub-kelompok interim per-lahan (#146, TD-013/TD-014)** — hierarki domain yang benar = **Petani → Kelompok Tani (Gapoktan) → Lembaga Petani**. Temuan owner: entitas `FarmerGroup` (relasi `Farmer.farmerGroupId`, tabel `tbl_farmer_group`) yang selama ini dilabeli "Kelompok Tani" **secara semantik = Lembaga Petani** (level teratas) — **mislabel**. Level Kelompok Tani & Gapoktan belum dimodelkan (data belum lengkap). **Keputusan:** (1) **Interim (#146, dikerjakan):** simpan sub-kelompok sebagai field denormalisasi **di `LandParcel`** — `subGroupLv1` (Gapoktan) + `subGroupLv2` (Kelompok Tani), nullable — **per-lahan** karena satu petani bisa punya beberapa lahan di KT/Gapoktan berbeda (opsi taruh di `Farmer` ditolak: 1 nilai/petani tak bisa multi). Backend+Zod+form manual+detail lahan; migrasi additif `20260714032307_add_land_parcel_sub_group` (via `migrate deploy`, **tanpa seed** — seed non-idempoten); build ✅ / test 380 ✅. (2) **Relabel UI (TD-013, #147):** label "Kelompok Tani" → "Lembaga Petani" di app + docs; **identifier tetap** `FarmerGroup` (rename massal ditolak, preseden #130); menu-key RBAC & field baru `subGroupLv2` (Kelompok Tani asli) tak diubah. (3) **Refactor penuh (TD-014, kemudian):** model KT/Gapoktan sebagai tabel via **Jalur B + B2** (rename `FarmerGroup`→`FarmerInstitution` + entitas baru, eksekusi export→rebuild→re-import saat data lengkap); blocker: keputusan 3 vs 4 level. **Docs disinkronkan** (models/erd/migrations/code-standards + relabel). |
| 2026-07-13 | **BUG-007 fix: scope-leak `getMapData` (Peta Lahan/MAP-01)** — follow-up dari audit #144. Pola key-collision yang sama dengan `getBmpMapData` diperbaiki: `farmerGroupAccessFilter(access)` dipindah dari **spread → `AND`** pada `groupWhere` di `map.ts`, sehingga literal `districtId` (wajib pada Peta Lahan) & `id` (KT opsional) **tak lagi menimpa** scope `{ districtId/id: { in } }` (mode BY_DISTRICT/BY_FARMER_GROUP). Dampak sebelum fix: user ber-scope bisa memuat District/KT **di luar assignment** via panggilan Server Action langsung (UI-bypass) — kini ditutup. **+3 replica test** `map groupWhere scope` (BY_DISTRICT/BY_FARMER_GROUP/ALL) di `map.test.ts` (31→34; gaya mirror sesuai `access-context.test.ts` karena modul asli menarik next-auth/prisma). **Gate:** lint **exit 0** (0 error), build ✅, **test 27 file / 380 ✅**. TD register **BUG-007 → ✅ Done**. |
| 2026-07-13 | **#144 MAP-02: Peta BMP — Layer 1 Ketersediaan Data Produksi (selesai)** — sub-menu peta **kedua** stream MAP: **Peta BMP** (`/admin/map/bmp`), peta tematik yang mewarnai tiap **lahan** menurut kelengkapan data produksinya. **4 kategori** dari **run bulan berturut-turut terpanjang** (`period` YYYY-MM unik & aktif): **Baik** > 24 bln (hijau), **Cukup** 12–24 (kuning), **Kurang** 1–11 (oranye), **Tidak ada** 0 record (abu). **Alur filter beda dari Peta Lahan:** **Lembaga Petani wajib**; Provinsi & Distrik **opsional** (hanya menyaring daftar KT — user bisa pilih KT langsung); tombol Muat Data disabled sampai KT dipilih. **Server action** `getBmpMapData` (`map.ts`, RBAC 3 layer: `hasPermission("map-bmp","VIEW")` + `getAccessContext` scope via `farmer→farmerGroup` + `isActive`); produksi diambil **1× untuk semua lahan** via `productionRecord.groupBy({by:['parcelId','period'], where:{parcelId:{in},isActive}})` lalu dikelompokkan di JS (**hindari N+1**, sesuai catatan #136 "groupBy scoped bukan IN per-lahan"). **Helper murni** baru di `map-data.ts`: `longestConsecutiveMonths` (dedup→index bulan absolut `y*12+m`→sort→run terpanjang; lintas-tahun `2025-12→2026-01` berturut) + `productionAvailabilityCategory` + konstanta ambang **`BMP_BAIK_MIN_MONTHS=24`/`BMP_CUKUP_MIN_MONTHS=12`** (mudah diubah owner) + `buildBmpMapData` (transform + counts, skip geometry null). Tipe (`BmpMapFilters`/`BmpParcelFeature`/`BmpMapData`/`ProductionAvailabilityCategory`) di `types/map.ts`; Zod `bmpMapFilterSchema` (`farmerGroupId` wajib). **Client** `src/app/(admin)/admin/map/bmp/` (page/loading/client/control-panel/canvas) — **file terpisah, tak menyentuh Peta Lahan** (patuh "jangan refactor MAP-01"); reuse dropdown fetcher (`getProvincesForMap`/`getDistrictsForMap`/`getFarmerGroupsForMap`) + basemap light/dark/hybrid + auto-fit. Canvas **data-driven color** MapLibre (`["match",["get","category"],…]` untuk fill+line+point) + visibilitas per-kategori via `filter` `["in",["get","category"],["literal",…]]` + point KT konteks (netral biru, tak dikategorikan) + popup lahan (badge kategori, run bulan, periode awal–akhir). **Keterbatasan diketahui:** `ProductionRecord.parcelId` nullable → produksi tanpa `parcelId` **tak diatribusikan** ke lahan mana pun (tak memengaruhi warna) → disclaimer di legend & popup (isu data-quality #136). **Read-only, tanpa tabel/migration baru.** Seed: **2 baris CSV ditambahkan** (`menu.csv` `map-bmp` order 2 icon `Sprout`; `role-permissions.csv` 4× VIEW SUPERADMIN/ADMIN/OPERATOR/MANAGEMENT), **sudah di-seed** (approval owner, `scripts/local/seed-menu-only.ts`, upsert idempoten, DB tidak di-reset). **+16 unit + 2 perf test** (`map.test.ts` 15→31; `perf.test.ts` `buildBmpMapData` 9,27ms / `longestConsecutiveMonths` 0,07ms). **Revisi lanjutan (detail lengkap di entri bulanan changelog & baris roadmap MAP-02):** rendering diubah ke **poligon-only tanpa titik** (KT & centroid point di-drop; **NONE outline-saja**, + **label nama petani** di poligon), ekspor **"Cetak Peta dan Matriks Ketersediaan Data" (PDF landscape)** & **Download Excel** (+kolom Status Ketersediaan & Luas), popup jadi **accordion + grafik Produksi Bulanan**, **panel kiri/kanan minimizable** + kontrol **basemap & "Zoom ke semua data" di kanan-bawah** (diterapkan juga ke MAP-01 + diresmikan di `ui-ux.md`), dan **fix scope-leak** `getBmpMapData` (`farmerGroupAccessFilter` → `AND`) — pola sama di `getMapData` MAP-01 dicatat **BUG-007**. **Gate:** lint **exit 0** (0 error, 3 warning baseline #126), **test 27 file / 377 ✅** (dari 359), build ✅ (route `/admin/map/bmp` terdaftar). Follow-up (#144 Open Q): layer BMP lain (kepatuhan pelatihan/produktivitas/sertifikasi) + selektor layer + strategi atribusi produksi tanpa `parcelId`. |
| 2026-07-12 | **#130 AUDIT-P3: Kualitas berkelanjutan (audit fields, Zod, naming, rename, font)** — item P3 dari audit 2026-07-10 §8 (TD-010 audit fields ✅, TD-012 naming ✅). **(1) Audit fields (TD-010b):** `createdBy`/`modifiedBy` kini diisi dari `auth()` session di mutasi yang sebelumnya kosong — `user.ts` (create/update/toggle), `menu.ts` (create/update/delete), `role-permission.ts` (toggle: create+update), `farmer.ts` (`toggleFarmerActive`), `region.ts` (4 `toggle*Active`), `user-data-access.ts` (3 `assignUser*` → `createdBy`), `user-menu-access.ts` (`setUserMenuOverride` upsert: create `createdBy` / update `modifiedBy`). Import `@/lib/auth` ditambah di 5 file yang belum. **(2) Zod (TD sisa):** `addParticipants` divalidasi `addParticipantsSchema` baru (activityId + array peserta `farmerId`+skor 0–100, min 1 peserta) — return `error:string` (bukan `fieldErrors`, sesuai konsumen `toast.error`); `changePassword` pakai `changePasswordSchema` baru (`profile.schema.ts`) menggantikan cek panjang inline. **(3) Naming (TD-012 — keputusan):** dipilih **resmikan istilah domain** (bukan rename massal) — `petani/lahan/pelatihan/produksi/lembaga petani (KT)/persil/paket` didokumentasikan sebagai pengecualian resmi aturan "variable English" di `code-standards.md`; nilai enum DB (`PAKET_1_*`) = data, di luar aturan. **(4) Konsistensi:** `src/types/land-parcel.types.ts` → `land-parcel.ts` (satu-satunya suffix `.types.ts`; 5 importer diperbarui). **(5) Font (keputusan owner → brand WRI):** body semula `"Acumin Pro Condensed","Inter"` tanpa `@font-face` → jatuh ke sans-serif generik; `--font-sans` self-referential padahal Geist dimuat. Diperbaiki ke **brand WRI Acumin Pro Condensed** (self-host `public/fonts/`, `@font-face` regular+bold, `font-display:swap`) + fallback **Arial/Helvetica** sesuai rekomendasi WRI (`brand.wri.org/fonts`); `--font-sans` diarahkan ke stack ini; Geist Sans (sans) dilepas dari `layout.tsx` (Geist Mono tetap untuk `--font-mono`). File font berlisensi belum di-commit — README panduan di `public/fonts/`. **Ditunda (sesuai issue — jangan refactor massal):** pemecahan file client besar (§5), `isActive` pada join-table assignment (hard-delete masih justified). **Test baru:** `profile.test.ts` (3) + `addParticipantsSchema` (6) di `training-participant.test.ts` + 1 perf test (`addParticipants` 2000 peserta = **4.86ms**, threshold 50ms). **Perf:** `auth()` = strategi JWT (tanpa DB hit) → tambahan `await auth()` di mutasi ≈ nol biaya. **Gate:** lint **exit 0** (0 error, 3 warning baseline #126), **test 27 file / 359 ✅** (dari 349, +10), build ✅. |
| 2026-07-12 | **#129 AUDIT-P2: Cleanup dead code, deps 0-usage & konsolidasi helper (TD-009, TD-011, TD-010 bagian ActionResult)** — PR cleanup terpisah; semua klaim 0-usage diverifikasi grep sebelum hapus. **(1) Deps `package.json`:** hapus 0-usage `@dnd-kit/core|sortable|utilities`, `adm-zip` (`shpjs` mem-parse buffer ZIP langsung), `react-hook-form`+`@hookform/resolvers` (hanya diimport `ui/form.tsx` yang dead), `recharts` (0 usage — pasang lagi saat chart produksi), `ts-node`+`@types/sharp` (devDeps; semua via `tsx`, sharp≥0.32 bundle types). **Pindah ke devDependencies:** `csv-parse` (hanya `prisma/seeds/**`), `sharp` (hanya `scripts/local/` gitignored; `next/image` 0 usage). **Tambah** `dotenv` (devDeps, untuk 2 CLI). `shadcn` **dipertahankan** (dipakai via `@import "shadcn/tailwind.css"` di `globals.css`). **(2) File/export mati:** hapus `lib/constants.ts` + 6 komponen (`ui/alert|breadcrumb|form|scroll-area|sonner`, `layout/placeholder-page`; `Toaster` diimport langsung dari paket `sonner`). `ui/input-group` **dipertahankan** (dipakai `command.tsx`). Hapus export mati `isS3Key`; de-export `DASHBOARD_PACKAGE_CODES` (internal-only); dedup tipe lokal `FarmerSelect` (`production-form-client` → pakai `types/land-parcel.types`). **(3) Konsolidasi helper (DRY):** `getFarmerGroupsForSelect` ×3 + `getFarmersForSelect` ×2 → satu modul ber-guard baru `src/lib/select-options.ts` (`getFarmerOptions`/`getFarmerGroupOptions`, param `menuKey` → `hasPermission(menuKey,"VIEW")` sesuai halaman; 7 call-site `page.tsx` diperbarui, definisi lama di `farmer/training/land-parcel/production.ts` dihapus). **Standardisasi `ActionResult<T>` (bagian payload ad-hoc):** `{success,granted}` (`role-permission.toggleRolePermission`) & `{success,count}` (3 `bulk-upload*`) → payload dipindah ke `data` + anotasi tipe `Promise<ActionResult<…>>`; 4 konsumen client memakai `result.data?.x`. `checkDuplicateProduction` sudah privat (tak berubah). **Ditunda (sengaja):** pola `error: parsed.error.flatten().fieldErrors` (22 titik di 9 action) **belum** diubah — field `error` di sini bersifat load-bearing (form membedakan `typeof error === "string" ? toast : setErrors(obj)` untuk render error per-field), jadi memisahkannya = perubahan kontrak + tipe `ActionResult` lintas ~10 form modal, di luar scope "diff bersih" P2; direkomendasikan sebagai follow-up khusus (lihat analisa #129). **(4) Env & tooling:** `.env.example` +`FIRMS_MAP_KEY_FREE` −`NEXT_PUBLIC_S3_PUBLIC_URL` (0 ref); `.dockerignore` +`.env`/`.env.*` (`!.env.example`); `require("dotenv/config")` di `get-link.js`+`pdf-manager.js`; `pdf-manager` perbaiki ternary identik `listTrainingPDFs` (kini filter `activityId` client-side — activityId di depth-4 key, tak bisa jadi S3 prefix) + hapus stub `cleanupOrphaned`+command+npm script `pdf:cleanup`; CLAUDE.md dikoreksi (`adm-zip` dihapus dari deskripsi bulk shapefile). **Keputusan owner:** `Dockerfile` **dipertahankan** (deploy `deploy-main`/`deploy-dev` build via SSH `npm run build`+pm2, bukan Docker — orphan tapi tak dihapus; cukup di-hardening via `.dockerignore`). **Gate:** lint **exit 0** (0 error, 3 warning baseline #126), **build ✅**, **test 26 file / 349 ✅**. |
| 2026-07-12 | **#128 AUDIT-P1: Konvensi UI (gating Menu Management, Combobox KT, loading.tsx)** — merapikan pemakai yang menyimpang dari konvensi UI `ui-ux.md` (komponen shared sudah patuh). **(1) Menu Management** (`settings/menu`): `menu-list-client` kini menerima prop `permissions` (di-fetch via `getUserPermissionsForMenu("settings-menu")` di `page.tsx`) → tombol **Tambah** digate `CREATE`, aksi baris via `<TableActions>` (gate `EDIT`/`DELETE`); kolom **Aksi dipindah ke paling kiri** + `w-[1%] whitespace-nowrap`; `confirm()` native diganti `<DeleteDialog>` shared (+ toast error). **(2) Combobox KT**: `master-data/farmers/farmer-form-modal` dropdown Lembaga Petani `<Select>` → **searchable Combobox** (Popover + Command), pola sama `training-form-modal`; `farmerGroupId` jadi state (bukan `FormData.get`). **(3) `loading.tsx`**: ditambah di 4 route tabel (`master-data/training`, `settings/menu`, `report/farmer`, `report/training`) dengan `<TableSkeleton>`. **(4) Kosmetik**: header Aksi `<TableSkeleton>` `w-[100px]` → `w-[1%] whitespace-nowrap` (kurangi layout shift); `tools/snapshot/snapshot-client` DataTable dibungkus `<Card>` konsisten dgn list lain. **Gate:** lint **exit 0** (0 error, 3 warning baseline #126), **test 26 file / 349 ✅**, build ✅. |
| 2026-07-12 | **#127 AUDIT-P1: Scope RBAC by-id + pola restore soft-delete seragam (TD-007)** — dua bagian. **A. Scope by-id + guard helper "for select".** Konsolidasi 2 helper baru di `access-context.ts` (`farmerAccessFilter`, `farmerRelationAccessFilter`; melengkapi `farmerGroupAccessFilter`) menggantikan ±25 ternary tulis-tangan di 5 file. Scope `getAccessContext` kini diterapkan di **read/mutasi by-id yang sebelumnya bolong**: `farmer-group.ts` (`getFarmerGroupById`/`updateFarmerGroup`/`toggleFarmerGroupActive`), `training.ts` (`getTrainingActivityById`/create/update/toggle + validasi `farmerGroupId` target, `getFarmersByGroup`, dan **peserta**: `addParticipants` cek scope activity + petani ∈ KT activity, `removeParticipant(s)`/`updateParticipantScores` via relasi `activity`), `land-parcel.ts` (`createLandParcel` validasi petani target + `updateLandParcel` cegah pindah petani luar scope). Guard `hasPermission` ditambah di semua helper "for select" (`getFarmerGroupsForSelect` ×2, `getDistrictsForSelect`, `getTrainingPackagesForSelect`, `getFarmersForSelect` ×2, `getAuditUserNames`); `checkDuplicateProduction` dijadikan **fungsi privat** (bukan endpoint). **Bug ditemukan & ditutup:** `{ id, ...farmerGroupAccessFilter(access) }` → filter `{ id: { in } }` (mode BY_FARMER_GROUP) **menimpa** literal `id` = scope bocor (mis. `createFarmer`/`getFarmerGroupById` bisa jangkau KT lain); diperbaiki ke pola `{ id, AND: farmerGroupAccessFilter(access) }` di 8 titik. **B. Keputusan produk pola restore (TD-007):** dipilih **tampilkan record nonaktif + badge Status + filter Status (default "Aktif") + toggle "Aktifkan kembali"** — **dibatasi ke SUPERADMIN**; user lain hanya mengakses record aktif (kolom & filter Status di-hide). Helper `isSuperAdmin()` (`rbac.ts`) dipakai server (list & read by-id → `...(isSuperAdmin ? {} : { isActive: true })`) & client (prop `isSuperAdmin` dari page → render kolom/filter Status). Mutasi tetap butuh `isActive: true`. `toggleLandParcelActive`/`toggleProductionRecordActive` ditambah + list Lahan/Produksi/Petani/KT/Pelatihan diberi filter Status (SUPERADMIN). **UX Lahan:** filter list diganti dari **Petani** → **Lembaga Petani** (konsisten dgn list lain; daftar petani ribuan) + kolom Lembaga Petani; server action baru `getFarmerGroupsForSelect` (guard `master-data-parcels` VIEW). **Test:** `access-context.test.ts` baru (18 test lintas-scope + guard collision `AND`); replica list-where di `farmer`/`training-activity`/`land-parcel.test.ts` disinkronkan (drop `isActive`). Pola didokumentasikan di `code-standards.md` §Soft-delete. **Gate:** lint **exit 0** (0 error, 3 warning baseline #126), **test 26 files / 346 ✅**, build ✅. |
| 2026-07-12 | **#126 AUDIT-P1: Gate QA lint hijau kembali (BUG-006)** — `npm run lint` **229 masalah (193 error) → exit 0 (0 error, 3 warning)**; build ✅ & test **25 file / 328** ✅ dipertahankan. **(1)** `eslint.config.mjs`: tambah `ignores: ["scripts/**"]` (CLI/maintenance lokal, bukan kode app; `scripts` juga sudah di-exclude `tsconfig`) → −42 masalah `no-require-imports`/any. **(2)** Semua `no-explicit-any` di `src/` diganti **tipe nyata**, bukan disable: `where: any` → `Prisma.<Model>WhereInput` (`report.ts`/`data-analyst.ts`/bulk-upload); geometri → `geojson` (`Geometry`/`Feature`/`Point`/`Position`); event & style maplibre → `react-map-gl` `LayerProps`/`MapLayerMouseEvent` + `maplibre-gl` `StyleSpecification`/`GeoJSONSource`; sisanya `unknown` + narrowing; mock test → `vi.mocked(...)`. **(3)** `react-hooks/set-state-in-effect` ×6 diperbaiki tanpa efek: sinkron tema basemap → **derive saat render** (`styleOverride ?? theme`), `mounted` navbar → `useSyncExternalStore`, reset popup on-data-change → adjust-state-during-render, reset farmerGroups/hotspot → dipindah ke change-handler. `react-hooks/static-components` ×4 (`region-list-client`): `ActionCell` di-**hoist** ke module-scope + props. Unused-vars/`prefer-const`/`no-unused-expressions` dibersihkan. **(4)** Efek samping tipe dari perubahan agen diperbaiki: field placeholder column-key `district`/`farmerGroupId` (DataTable `key: keyof T`), `parcelArea` hilang di 1 literal test, cast `results.data as RawRow[]` (papaparse File-overload melebar ke `unknown[]`). **Keputusan:** sesuai AC #126, **tidak** ada penonaktifan rule global (hanya ignore `scripts/**` yang diizinkan); **3 warning `exhaustive-deps`** (`parcel-bulk-upload-map`/`parcel-map-view` zoom-on-load, `training-report-client` memo) **sengaja ditahan** karena menambah dep berisiko regresi (re-zoom/re-compute) — disepakati terpisah. Build ✅, test 25/328 ✅. |
| 2026-07-12 | **#125 AUDIT-P0: Remediasi keamanan RBAC selesai** — 6 celah guard/scope dari audit 2026-07-10 ditutup (BUG-003/004/005 → Done). **(1)** Guard `hasPermission` ditambah: `role-permission.ts` (`getRolePermissions` VIEW / `toggleRolePermission` EDIT `settings-roles` + **tolak perubahan role SUPERADMIN**), `menu.ts` (`createMenuItem`/`updateMenuItem`/`deleteMenuItem` CREATE/EDIT/DELETE `settings-menu`; `getAllMenuItems` VIEW **dual-key `settings-menu` OR `settings-roles`** karena dipakai halaman Menu & Roles), `upload.ts` (`uploadTrainingEvidence` `master-data-training` CREATE **atau** EDIT — edit kegiatan lama juga boleh unggah). **(2)** Scope `getAccessContext` diterapkan di `farmer.ts`: `getFarmerById`/`updateFarmer`/`toggleFarmerActive` (filter relasi KT/distrik; toggle sengaja tanpa `isActive` agar restore tetap jalan) + validasi `farmerGroupId` target dalam scope di `createFarmer`/`updateFarmer`, dan `bulk-upload.ts` `bulkCreateFarmers` (validasi semua KT target — **1 `findMany` + Set O(1)/baris**, bukan N+1). **(3)** `settings/roles/page.tsx` menuKey `settings-users`→`settings-roles`. **Keputusan teknis:** helper scope relasi-petani **tetap inline** (samai kode sekitarnya) — konsolidasi `farmerRelationAccessFilter` ditunda ke #129 (rule.md L116); `farmerGroupAccessFilter` existing dipakai untuk scope KT. **Test:** `rbac-server-guards.test.ts` (+15) + 2 perf test hot-path bulk-scope (`perf.test.ts`; 10k baris **0.43ms**, short-circuit terverifikasi). Build ✅, **test 25 files / 328 ✅**; lint belum disentuh (BUG-006/#126). Sisa scope by-id KT/pelatihan + pola restore → #127. |
| 2026-07-11 | **#134 Produksi Peta & PDF + #135 Panel Daftar Lahan selesai** (permintaan langsung, di luar #113; data produksi kini tersedia di MIS). **#134:** (1) popup Peta Lahan buang grafik dummy → **produksi asli per-lahan** via server action baru `getParcelProduction` (3-layer RBAC: `hasPermission("map-parcel","VIEW")` + scope KT + `isActive`) — **by `parcelId`**, bukan by farmer; (2) **selektor Rata-rata/tahun** (Shadcn Select) + grafik dengan **sumbu-Y kanan** (0·tengah·atas, atas = ceil kelipatan 100) + **tooltip hover** per bar; (3) helper murni baru `summarizeProduction` di `map-data.ts` (per-tahun + rata-rata lintas tahun, reuse `monthlyAverageYield`) + tipe `ProductionSummary`/`ProductionYear`; (4) **Farm Passport PDF**: grafik bar → **tabel matriks** baris=tahun × kolom=bulan × kolom kanan **Total** (`jspdf-autotable`), rename **"Farm Passport/Profil Petani" → "Profil Lahan"** (header, subjudul, nama file, tombol popup, toast); (5) **fix bug popup tidak ter-refresh** saat pindah lahan → `key` per fitur pada `<Popup>` (sekaligus fix laten section Pelatihan); (6) **dedup fetch produksi** — `getParcelPassport(landParcelId, includeProduction=true)` bisa skip query produksi + komponen `ParcelPopupBody` memegang state produksi bersama section & tombol PDF (fetch ≤1× per popup, tak memperburuk jalur download-tanpa-buka). **#135:** panel kanan **Daftar Lahan** (toggle) berisi lahan hasil Muat Data — **text search** (nama/ID petani/ID lahan) + tabel dengan **kolom Aksi paling kiri = zoom ke lahan** (`geomBounds`→`fitBounds` + buka popup). **Tambahan:** section **Legenda dibuat collapsible** (pola sama Filter/Peta Lainnya). +4 unit test `summarizeProduction` (`map.test.ts` 11→15) + 1 perf test (`perf.test.ts`; ~60k record/1 lahan **9ms**, threshold 50ms). Tech debt dicatat di **#136** (Recharts, data-quality produksi tanpa parcelId, debounce/virtualisasi panel, a11y, lahan tetangga PDF/#134-E, warna area lahan by ada/tidaknya produksi — ditunda, kemungkinan ada menu peta produksi lebih spesifik + catatan pakai `groupBy` scoped bukan `IN`). Build ✅, **test 24 files / 311 ✅**; lint 0 error baru (4 sisa = baseline audit #126). |
| 2026-07-11 | **#132 RPT-03: Report Produksi selesai** — Laporan Produksi baru di menu Report (sejajar Petani & Pelatihan): matriks produksi bulanan **per petani/lahan × bulan** dalam satu KT (format "Catatan Produksi Petani ICS"). Menu `report-production` (menu.csv + role-permissions.csv untuk SUPERADMIN/ADMIN/OPERATOR/MANAGEMENT), halaman `/admin/report/production` dengan `requirePermission`, server action `getProductionReport` di `report.ts` (3-layer RBAC: `hasPermission("report-production","VIEW")` + `getAccessContext` scope KT + `isActive`), helper murni `lib/report-production.ts` (`enumeratePeriods`/`formatPeriodLabel`/`buildProductionMatrix`, **14 unit test** di `report.test.ts`). **Keputusan teknis:** (1) filter Distrik→KT wajib + Periode Awal/Akhir via `<input type="month">`, kolom bulan dibangkitkan dinamis dari rentang (maks **24 bulan**), header `MMM-YY`; (2) satu query `findMany` di-filter `period` string-range (YYYY-MM lexicografis = kronologis) lalu pivot di memori jadi `Map<farmer::parcel, period→kg>`, panen ganda/bulan dijumlahkan; (3) sel kosong ditampilkan kosong (bukan 0), total per baris + baris "Total per Bulan"; (4) ekspor **Excel** (kolom dinamis + baris total) & **PDF landscape** (file siap unduh, bukan print browser) — `exportToPDF` di `lib/pdf.ts` dibuat orientation-aware + param opsional `headFontSize`/`bodyFontSize`/`cellPadding`/`columnStyles` (default lama dipertahankan agar report Petani/Pelatihan tak berubah); produksi pakai font 7 (6 bila >12 bulan) + lebar kolom identitas tetap + angka rata-kanan → 12 bulan muat 1 halaman A4 landscape tanpa angka/header ter-wrap; angka format id-ID, sel kosong tetap kosong; (5) kolom **Blok** pada mockup dilewati karena `LandParcel` belum punya field blok; (6) kolom **Luas (Ha)** ditambahkan setelah Id Lahan (dari `LandParcel.area`, format id-ID 2 desimal, `-` bila kosong) beserta total luas di baris footer — tabel/Excel/PDF; kolom Id di PDF pakai font 6 agar kode panjang tetap 1 baris. Perf test pivot ~48k record (500 petani × 2 lahan × 24 bulan × 2 panen) **< 150ms** (aktual ~31ms). Build ✅, **test 24 files / 306 ✅**. |
| 2026-07-10 | **AUDIT MENYELURUH codebase vs docs** — dijalankan atas seluruh folder/file/code (22 server actions dibaca penuh, 35 pages, lib/deps/config, drift 5 file docs). Hasil: test 24/296 ✅, build ✅, **lint ❌ 190 error**; **6 temuan HIGH** (guard `hasPermission` absen di `role-permission.ts`/`menu.ts`/`upload.ts`; scope absen `getFarmerById`/`bulkCreateFarmers`; gate lint merah) + 21 MED + ±25 LOW (dead code/deps). **Keputusan:** (1) remediasi P0 keamanan sebelum fitur baru (BUG-003/004/005); (2) barrel `@/components/shared` diresmikan sebagai pengecualian tunggal di rule.md; (3) klaim "14/14 compliant" dicabut, diganti tabel compliance 2026-07-10 (7 PASS/4 PARTIAL/3 FAIL); (4) keempat docs disinkronkan (angka test/fase/LOC, migrasi DB, menu, arsitektur, `shapefile`→`shpjs`, revision `@default(0)`, blok legacy ui-ux-flow dihapus); (5) laporan lengkap disimpan **di luar git** di `audit-report/audit-2026-07-10.md` (folder di-gitignore). Pending decisions: pola restore soft-delete; nasib `recharts` & `Dockerfile`. |
| 2026-07-10 | **MAP-01 enhancement — Titik Api (Hotspot) + Ruler + Label KT/Petani** (permintaan langsung, di luar scope #113). Layer hotspot **NASA FIRMS VIIRS 375 m** via proxy same-origin baru `src/app/api/map-hotspot/route.ts` (`runtime=nodejs`, validasi `bbox`+`dayRange`, cache 1 jam, `FIRMS_MAP_KEY_FREE` server-only, **auth guard `hasPermission("map-parcel","VIEW")`**). **Keputusan teknis:** (1) window **24 jam / 5 hari** — FIRMS free tier menolak `dayRange>5` (`Expects [1..5]`), jadi opsi "7 hari" awal diturunkan ke 5 (sempat dicoba 2-panggilan untuk 7 hari lalu disederhanakan atas permintaan "sesuaikan data available"); (2) area query dikunci **bbox Riau** (`RIAU_BBOX`); (3) titik diwarnai by kebaruan (<24 jam merah / 1–5 hari oranye) + popup detail + disclaimer "deteksi anomali panas, bukan konfirmasi kebakaran" + atribusi **NASA FIRMS**. **Ruler** (tanpa dependensi baru): ukur jarak & luas **geodesik** (haversine + spherical-excess) multi-titik, label per-segmen, fill poligon, undo/hapus/Esc. **Label**: nama KT (titik) + nama petani (poligon, **hanya bila teks muat di poligon pada zoom aktif**, wrap otomatis; `geomBounds` dihitung sekali per dataset). **Glyph single-font** `["Open Sans Regular"]` (patuh `rule.md:346`; "Noto Sans Regular" tak dilayani server). Logika murni diekstrak ke `src/lib/firms.ts` + `src/app/(admin)/admin/map/parcel/map-geo.ts` (+ client `map-hotspot.ts`) dengan **+22 unit test** (`firms.test.ts`, `map-geo.test.ts`). Perf: route cold ~1,7s (FIRMS-bound), warm ~5ms (cache). Follow-up: proximity alert KT/parcel↔hotspot, integration test route handler, clustering musim kering, sumber ganda (NOAA-20). Build ✅, **296 test** ✅. |
| 2026-07-09 | **MAP-01 #113 — section "Tambah Data GIS Lain" (bring-your-own GIS)** ditambahkan ke Peta Lahan. User bisa menambah layer sendiri via 3 mode: **WMS URL** (raster), **ZIP Shapefile** & **GeoJSON** (vektor). Shapefile/GeoJSON **diparse di browser** (`shpjs` dynamic import / `JSON.parse`) → render `<Source geojson>` (fill+line+circle). **Keputusan teknis:** WMS user di-fetch **langsung tanpa proxy** untuk hindari open-proxy/SSRF (server WMS harus CORS-enabled) — beda dari overlay SIGAP yang di-whitelist proxy. Layer bersifat session-only (tak dipersist). Auto-fit ke bounds layer vektor baru; `onError` handler pada `<Map>` agar tile gagal tidak jadi error fatal. File: `map-custom-gis.tsx` (baru) + helper di `map-overlays.ts` (`buildWmsTileUrl`/`toFeatureCollection`/`geojsonBounds`) + `map-canvas.tsx`/`map-control-panel.tsx`/`map-parcel-client.tsx`. |
| 2026-07-09 | **MAP-01 #113 — section "Peta Lainnya" (overlay referensi) ditambahkan** ke Peta Lahan. Overlay raster tematik dari SIGAP KLHK/Kementerian Kehutanan (ArcGIS REST `export`): Kawasan Hutan, Pelepasan Kawasan Hutan, Fungsi Ekosistem Gambut, PIPPIB/Moratorium, Penutupan Lahan 2022 — toggle per-layer + slider transparansi, di-render di bawah layer data petani. **Keputusan teknis:** tile di-proxy same-origin via route baru `src/app/api/map-overlay/[key]/route.ts` karena server KLHK tidak mengirim header CORS dan TLS chain tak lengkap; ini pengecualian sempit atas aturan "no REST API layer" (endpoint gambar biner tak bisa jadi Server Action), dengan whitelist per-overlay + cache 24 jam. Overlay bersifat **referensi visual**, bukan dasar penetapan resmi. Follow-up terpisah: analisis spasial akurat (overlap parcel ↔ kawasan hutan via PostGIS `ST_Intersects`, ingest data resmi ke DB). File: `map-overlays.ts` (baru), `map-canvas.tsx`, `map-control-panel.tsx`, `map-parcel-client.tsx`, `api/map-overlay/[key]/route.ts` (baru). |
| 2026-07-08 | **Stream `MAP` ditambahkan + Issue #113 [MAP-01] dibuat** — menu Level-1 baru **Map** + sub-menu **Peta Lahan** (`/admin/map/parcel`). Scope scaffolding: peta full-bleed MapLibre (`react-map-gl`), panel filter floating kiri (Province→District→KT, collapsible, tombol Muat Data), legend layer toggle (Point KT / Point centroid lahan / Area polygon lahan), klik feature → info floating. Read-only dari `FarmerGroup` (`locationLat/Long`) + `LandParcel` (`geometry`) — **tanpa tabel/migration baru**; centroid lahan dihitung dari polygon via `@turf`. Filter District wajib (batasi payload). RBAC 3 layer (`hasPermission("map-parcel","VIEW")` + `getAccessContext` scope via `farmer→farmerGroup→district` + `isActive`). Estimasi 20–28 jam. Draft: `draft-issue/issue-map-peta-lahan-map-01.md`. |
| 2026-07-06 | **3 GitHub Issues dibuat untuk Report module** — #107 (RPT-01 Report Petani), #108 (RPT-02 Report Pelatihan), #109 (RPT-03 Report Produksi). Scope diputuskan: filter Distrik + KT wajib dipilih (tidak ada "Semua"), filter opsional Periode di RPT-03, Export Excel per laporan. Estimasi total 50–64 jam (3 issues). Dependencies: RPT-01 → RPT-02, RPT-03 (RPT-02 & RPT-03 extend `report.ts` dari RPT-01). |

</details>

<details>
<summary><strong>Juni 2026</strong></summary>

| Tanggal    | Keputusan                                                                                                                      |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 2026-06-25 | Issue #94 dibuat untuk tambah field Pre-Test dan Post-Test Score pada Training Participant: `preTestScore` & `postTestScore` nullable Integer fields (range 0-100) untuk track evaluasi peserta sebelum dan sesudah pelatihan. Schema, validation, UI (form input + table column), dan bulk upload enhancement. Estimasi 4-6 jam (1 hari kerja). |
| 2026-06-12 | **CODE AUDIT COMPREHENSIVE** — Audit lengkap seluruh codebase (src/, prisma/, tests/) dan update keempat dokumen utama (progress.md, rule.md, database-schema.md, ui-ux-flow.md) berdasarkan state aktual: 13 test files/155 tests passing ✅, Training module MD-05 fully implemented ✅, Bulk Upload farmers BULK-03 complete ✅, server actions 1600 LOC total, validation schemas 6 files. Status fase diverifikasi ulang terhadap route/schema/action/UI yang benar-benar ada. |
| 2026-06-12 | Database Schema Documentation (database-schema.md) enhanced dengan P0 critical sections: Index Strategy (primary/secondary indexes, performance targets), Constraint & Data Integrity (FK cascade behaviors, business rules, soft delete, referential integrity check), Migration Strategy (workflow diagram, risk levels, history, pre-deployment checklist, breaking changes policy, backfill strategy), Security Considerations (auth/RBAC, password bcrypt, SQL injection prevention, data access patterns, sensitive data protection, audit trail, PostgreSQL access control, environment variables, OWASP Top 10 compliance), dan Performance & Data Volume (3-year projections, table size estimates Year 3 ~112 MB, critical query optimization targets < 300ms, pagination strategies offset vs cursor, N+1 prevention, connection pooling config, caching strategy per data type, future optimization considerations). Dokumentasi sekarang production-ready dan compliance dengan security/performance best practices. |
| 2026-06-11 | Issue #87 dibuat untuk PLATFORM-07: Hierarchical Menu Enhancement (3-Level Support) — untuk manage detail sub menu seperti Training Participants, Farmer Parcel/Training/Production, dengan RBAC inheritance logic dan validation depth max 3. Estimasi 12-16 jam (2-3 hari kerja). |
| 2026-06-06 | `progress.md` disinkronkan ulang berdasarkan existing file/code, bukan changelog historis.                                     |
| 2026-06-06 | DASH-01/DASH-02/DASH-03/DASH-04 diturunkan statusnya karena dashboard source masih placeholder dan action dashboard tidak ada. |
| 2026-06-06 | MD-03–MD-08 dikonfirmasi belum implementatif karena tidak ada Prisma model, route, server action, validation, atau UI.         |
| 2026-06-06 | BUG-001 ditambahkan untuk redirect `/admin/master-data` ke route missing `/admin/master-data/farmers`.                         |
| 2026-06-06 | BUG-002/TD-005 ditambahkan untuk stale dashboard scripts yang refer ke action dashboard missing.                               |
| 2026-06-06 | OPS-01 dinilai Partial karena test tersedia dan lulus 111/111, tetapi coverage belum mencakup modul baru/dashboard.            |
| 2026-06-06 | OPS-02 dinilai Partial karena Dockerfile dan GitHub deploy workflows ada, tetapi deployment readiness belum diverifikasi.      |
| 2026-06-06 | Stream RPT (Report) dan BULK (Bulk Upload) ditambahkan ke Phase Encoding Taxonomy.                                             |
| 2026-06-06 | 9 GitHub Issues dibuat (#62–#70) untuk Dashboard, Report, dan Bulk Upload.                                                     |
| 2026-06-06 | RBAC sementara SUPERADMIN-only untuk Dashboard, Report, Bulk Upload; role lain via User/Menu Management.                       |
| 2026-06-06 | Dashboard scope diputuskan: summary cards + filter district (bukan chart/map).                                                  |
| 2026-06-06 | Report scope diputuskan: Excel only di Phase 1, PDF ditunda.                                                                   |
| 2026-06-06 | Bulk Upload scope diputuskan: KT implementasi penuh, Region placeholder dulu.                                                  |
| 2026-06-06 | Tambah phase PLATFORM-06 dan buat Issue #71 untuk refactor list tabel ke DataTable dan integrasi Excel export + show/hide kolom. |
| 2026-06-07 | MD-03 Farmer scope diputuskan: MVP Phase 1 tanpa CSV import, NIK optional, Village optional, focus CRUD + RBAC + UI.           |
| 2026-06-07 | MD-03 Farmer breakdown: #72 (schema), #73 (actions+validation), #74 (UI), #75 (docs). Total estimasi 8-12 jam development.     |
| 2026-06-08 | BULK-03 Farmer scope diputuskan: Template-less Excel upload dengan column mapping UI, auto-matching logic, smart validation (gender normalization, date parsing, NIK), preview table dengan filter valid/error, download invalid rows only untuk user perbaiki, bulk insert transaction-based. Issue #76 dibuat dengan estimasi 20-28 jam (3-4 hari kerja). |
| 2026-06-10 | Issue #86 dibuat untuk tambah field joinedYear (Tahun Bergabung) pada master data Petani: schema, validation, CRUD, UI (form/list/detail), bulk upload, tests. Field optional Integer 1900-2100. Estimasi 4-6 jam (1 hari kerja). |
| 2026-06-10 | MD-05 Training selesai dengan target #77-#82 (schema, actions, UI list & detail, participant management, dan tests) menggunakan tipe enum TrainingCategory baru. |

</details>

### Changelog

<details>
<summary><strong>Juli 2026</strong></summary>

| Tanggal | Perubahan |
| ------- | --------- |
| 07-22   | **#188 polish (TD-028) ✅** — Peta Lahan & Peta BMP dimigrasikan ke primitif popup bersama `map-popup.tsx` (hapus salinan lokal `PopupHighlight`/`PopupSection`/`AttrRows` + impor menganggur) → popup peta benar-benar satu sumber. Header **Sebaran Lahan** kini setara Peta Lahan (ID Petani + ID Lahan + Lembaga Petani via payload `farmerCode`/`farmerGroupName`); popup ditutup otomatis pasca-edit; **fix lebar**: popup `w-max` + ID mono `whitespace-nowrap` agar ID panjang tampil penuh satu baris. Gate lint 0 / build / test 675 ✅. |
| 07-22   | **#188 Popup lahan + aksi peta + STANDAR popup ✅** — popup **Sebaran Lahan** dirombak ke gaya popup standar (dulu default maplibre: close menimpa header, teks terpotong). Primitif popup diekstrak ke **`components/shared/map-popup.tsx`** + jadi **rule** di `docs/standards/ui-ux.md`. **Footer aksi** `ParcelPopupActions` di **3 peta** (Sebaran Lahan, Peta Lahan, Peta BMP): **Lihat Detail** (gate VIEW) + **Edit Lahan** (buka `ParcelFormModal`, gate EDIT); Peta Lahan tetap Profil Lahan, Peta BMP dapat footer baru. Edit lazy-load via `ParcelEditModalHost` (+ action `getParcelFarmerOptions`); refresh peta pasca-simpan (`router.refresh()` / callback refetch). Bantuan Peta Lahan diperbarui. Gate lint 0 / build / **test 675** ✅. |
| 07-22   | **#187B Perombakan UI Settings ✅** — Menu Management & Role & Permission kini **render rekursif 3 level** (helper `src/lib/menu-tree.ts`) — **memperbaiki bug menu level-3 yang tak pernah muncul di matriks**. **Collapsible per induk** (default collapsed, `localStorage`, Buka/Tutup semua). Role & Permission: **header + kolom Menu sticky**, **selektor role**, **search**, **toggle baris penuh** + **kaskade induk→anak** (dialog konfirmasi), **feedback optimistis** via `setRolePermissions(updates[])` (ganti `router.refresh()` per klik). **SUPERADMIN dikecualikan** dari matriks (selalu akses penuh). +9 test `menu-tree.test.ts`. Gate lint 0 / build / **test 675** ✅. |
| 07-22   | **#187A Role `DONOR` ✅** — enum `Role.DONOR` (migrasi `20260722010000`) + sentralisasi daftar role ke `src/lib/roles.ts` (mengganti 5 hardcode: schema, form pengguna, badge daftar pengguna, matriks Role & Permission). Hak akses **VIEW-only** Dashboard + Report + Map + Bantuan (tanpa Master Data/Bulk Upload/Tools/Settings), ekspor tetap boleh; data individu petani sementara terlihat seperti MANAGEMENT (pemisahan agregat menyusul). Docs Bantuan + status 4→5 role. Bagian B (perombakan UI Settings: rekursif 3-level, collapsible, sticky, selektor role, toggle baris/kaskade, search — termasuk fix bug level-3 di matriks) ditunda sebagai issue terpisah. Gate lint 0 / build / **test 666** ✅. Migrasi + seed role-permission **diterapkan ke `mis-prod`** (15 baris DONOR terverifikasi di DB). |
| 07-21   | **Rilis `v0.15.0`** — MINOR: fase **DASH-06 Dashboard Pelatihan** & **HELP-02 Bantuan: Tutorial per Tugas** Done. DASH-06: live query (bukan snapshot) — 5 KPI, matriks cakupan Lembaga × Paket ber-drill-down ke daftar petani belum dilatih (salin/Excel), tren, panel pre/post & kualitas data. HELP-02: Bantuan dirombak jadi **tiga lapis** (17 tutorial per tugas dengan dua tingkat kedalaman dari satu sumber · 4 referensi halaman · 11 konsep), toggle Ringkas/Detail & lipat daftar isi tanpa JS, personalisasi per izin, bantuan kontekstual `HelpHint`, font Light 300. Plus **TD-020…TD-025** ditutup — termasuk **ID Petani unik per Lembaga** (`@@unique` + migrasi, sekaligus menutup celah scope `getExistingFarmerIds` yang membocorkan ID lintas wilayah kerja) dan perbaikan **logout yang tidak pernah mengakhiri sesi**. Audit pra-rilis menemukan materi Bantuan yang bertentangan dengan perubahan di rilis yang sama — diperbaiki sebelum tag. Bump `package.json` `0.14.0` → `0.15.0`; PR `mvp` → `main` + tag annotated `v0.15.0` + GitHub Release dengan notes dari changelog. |
| 07-21   | **Lapis Referensi + TD-020/TD-021 ✅** — **Lapis ketiga Bantuan aktif**: 4 halaman tersering (Petani, Lahan, Pelatihan, Produksi) dipanen dari katalog `docs/product/pages/` jadi materi "arti tiap kolom, filter, dan tombol" — berbasis definisi, untuk dirujuk saat bekerja, bukan dibaca berurutan; tampil sebagai seksi tersendiri di indeks. **TD-020**: perf test atas fixture **60.000 baris kehadiran** (~7× volume hari ini) — KPI+matriks+tren+skor <1.200 ms, plus test kebenaran; ambang merah = sinyal menimbang snapshot, bukan melonggarkan ambang. **TD-021 (sebagian)**: hook bersama `use-url-filters.ts` (`router.replace` + `scroll:false`, nilai kosong dihapus dari query) diterapkan di Dashboard Pelatihan — filter kini bisa di-bookmark & dikirim; Main & BMP Dashboard menyusul. **Migrasi TD-024 diterapkan ke mis-prod** (`@@unique` terverifikasi ada di DB). Gate lint 0 / build / **test 663** ✅. |
| 07-21   | **TD-024 & TD-025 ditutup + Bantuan kontekstual + 4 tutorial ✅** — **TD-024**: `farmerId` kini **unik per Lembaga** (keputusan owner) — `@@unique([farmerGroupId, farmerId])` + migrasi `20260721060000`, cek duplikat di `createFarmer`/`updateFarmer` dengan pesan per-kolom yang membedakan duplikat aktif vs milik petani nonaktif; `getExistingFarmerIds` yang tadinya mengambil **seluruh** ID di database tanpa filter (lebih ketat dari aturan sistem **dan** membocorkan ID di luar wilayah kerja) kini per-lembaga + verifikasi scope, sehingga halaman bulk upload memuat daftarnya setelah lembaga dipilih. Prasyarat diverifikasi read-only: 3.448 baris, 0 duplikat. **TD-025**: mode Detail tak lagi bergantung urutan sumber CSS (penanda `data-depth` → spesifisitas 0,7,0 vs 0,4,0). **Bantuan kontekstual**: komponen `HelpHint` (ikon `?` → tutorial menu ybs, tab baru) di 7 halaman Master Data & Bulk Upload. **+4 tutorial** (BMP Dashboard, Peta Lahan, Analisa Ketersediaan Data, User Management) → **17 tutorial / 5 bab**, seluruh menu utama tercakup. Gate lint 0 / build / **test 661** ✅. |
| 07-21   | **fix(help): 9 perbaikan dari review putaran 2 ✅** — **(1) 5 materi keliru** ditemukan dengan mengadu tutorial ke kode: form Petani ternyata **tidak memeriksa duplikat** ID (tutorial menjanjikan perlindungan yang tak ada), dan bulk upload memeriksa **seluruh database** bukan per Lembaga — kebalikan dari yang tertulis; halaman Lahan tak punya kartu ringkasan; tombol Produksi berbunyi "Tambah Data"; Periode adalah pemilih bulan bukan ketikan `2026-06`; Panen Ke- dibatasi 4. **(2) A11y**: `peer-focus-visible` di `<label>` bersarang → **tidak ada indikator fokus keyboard** (pitfall yang sama terlewat sekali lagi); lipat daftar isi bisa dipicu di bawah `lg` tempat tombolnya tersembunyi → jebakan satu arah, kini dikurung `lg:`; nama aksesibel disamakan dengan label terlihat (WCAG 2.5.3). **(3) Pencarian** yang menemukan teks Detail kini membuka `?detail=1` — sebelumnya kata yang dicari tak terlihat, bahkan oleh Ctrl+F. **(4)** `permission` frontmatter kini benar-benar dipakai (`getEffectiveMenuPermissions`) — sebelumnya divalidasi test tapi tak berpengaruh; dipindah ke modul murni `help-access.ts` agar teruji. **(5)** `generateStaticParams` dihapus (halaman selalu dinamis karena `auth()`). Dua temuan tingkat aplikasi dicatat sebagai **TD-024** (keunikan `farmerId` + celah scope `getExistingFarmerIds`) dan **TD-025**. Gate lint 0 / build / **test 645** ✅. |
| 07-21   | **HELP-02 Bantuan: tutorial per tugas ✅** — redesain 3 lapis (tutorial/konsep/referensi, rute lama tetap hidup); **13 tutorial dalam 4 bab** dengan dua tingkat kedalaman dari satu sumber (baris `+` = Detail) + toggle Ringkas/Detail tanpa JS; indeks dibuka "Apa yang ingin Anda lakukan?"; tiap tutorial punya tombol ke halamannya (tab baru); daftar isi bisa dilipat (komponen bersama `help-layout.tsx`); personalisasi peran (tutorial di luar akses ditandai); parser `markdown-lite` +blok `steps`/`callout`/detail; font **Light 300 sungguhan** (`.otf`→`.woff2`, 80→42 KB) didaftarkan di `globals.css`; penjaga kelengkapan materi di `help-content.test.ts` (langsung menangkap 2 tutorial tanpa `href`). Gate lint 0 / build / **test 640** ✅. |
| 07-21   | **fix: TD-022 & TD-023 ditutup ✅** — **TD-022**: `bulk-upload-parcel.ts` kini me-*repoint* seluruh `ProductionRecord.parcelId` dari lahan lama ke lahan revisi di dalam transaksi yang sama. Sebelumnya revisi lahan (nonaktifkan lama → buat baris baru ber-id baru) memutus atribusi produksi: tonase tetap masuk pembilang produktivitas BMP, luasnya tidak masuk penyebut, dan lahan terbaca "tanpa data produksi". `updateLandParcel` tidak terdampak (update in-place). Bagian `parcelId = null` **sengaja dipertahankan** — keputusan owner #136, bukan celah. **TD-023**: divergensi definisi cakupan ternyata **mustahil terjadi** — `addParticipants` sudah memvalidasi peserta ke Lembaga penyelenggara dan menolak seluruh batch bila ada yang tidak cocok; digabung filter `farmer.isActive` di DASH-06, kedua definisi terbukti identik. Diverifikasi ke `mis-prod`: 17.063 record produksi **0 orphan**, 8.240 kehadiran **0 lintas-Lembaga** → tidak ada perbaikan data retroaktif. +7 test invarian hulu (total 18 di `dashboard-asymmetry.test.ts`). Gate lint 0 / build / **test 586** ✅. |
| 07-21   | **test: invarian cakupan lintas 3 dashboard ✅** — `src/test/dashboard-asymmetry.test.ts` (+11 test) menahan kelas cacat "pembilang memakai filter berbeda dari penyebut" yang sempat rilis di DASH-06. **Main**: cakupan ≤ jumlah petani, kegiatan nonaktif tidak menambah pembilang, hadir berulang tetap 1 petani. **BMP**: `lahanBerData ≤ totalLahan` & `petaniMelapor ≤ totalPetani` walau produksi menunjuk lahan tak dikenal; produktivitas yang menggelembung **dipaku sebagai test karakterisasi** (TD-022) sehingga perubahannya harus disengaja. **Pelatihan**: kontrak payload — peserta di luar penyebut membuat cakupan >100%, merekam kenapa filter `farmer.isActive` + keanggotaan di action wajib ada. Menggantikan pemeriksaan manual `scripts/local/audit-*.ts` dengan pemeriksaan di gate. Gate lint 0 / build / **test 579 (43 file)** ✅. |
| 07-21   | **fix(DASH-06): 5 cacat dari review putaran 2 ✅** — (1) **pembilang cakupan bocor**: peserta di payload tidak memfilter `farmer.isActive` maupun keanggotaan Lembaga padahal penyebut memfilter keduanya → sel bisa tembus >100%, `gap` jadi 0, sel **tidak bisa diklik**, dan petani yang benar-benar belum dilatih tak pernah terjangkau; kini `where: { isActive, farmer: { isActive } }` + filter `farmerGroupId` di JS agar sama persis dengan `getUntrainedFarmers`. (2) Tooltip kolom "Lainnya" mencetak `target null% tercapai` (paket itu memang tanpa target) → cabang terpisah. (3) `exportToExcel` sudah menambah `.xlsx` sendiri → nama berkas `…xlsx.xlsx`; sekalian sanitasi nama Lembaga (`/` merusak nama unduhan). (4) Ringkasan "kurang N menuju target" dijumlah hanya atas paket ber-kegiatan → **non-monoton**: mencatat kegiatan paket baru justru menaikkan angka kekurangan; kini atas seluruh paket bertarget. (5) `packageCode`/`year` dari klien tidak divalidasi → `PrismaClientValidationError` 500; ditambah guard (bukan Zod — konvensi repo untuk read action berparameter skalar). Persen dibulatkan ke bawah agar 999/1.000 tidak terbaca "100%". +1 test regresi monotonisitas; gate lint 0 / build / **test 568** ✅. |
| 07-21   | **DASH-06 lanjutan: target cakupan + drill-down petani belum dilatih ✅** — sel matriks yang belum mencapai target kini **bisa diklik** → modal daftar petani yang belum mengikuti paket tsb (Nama + ID Petani + L/P, **tanpa NIK**) dengan tombol **Salin** dan **unduh Excel**, siap dipakai sebagai daftar undangan; action `getUntrainedFarmers` on-demand (payload awal tetap ramping) dengan scope `AND: farmerGroupAccessFilter(...)` anti key-collision. **Target program `TRAINING_COVERAGE_TARGET` = 100% petani aktif per paket** (konstanta di lib, `OTHER` tanpa target) — warna sel & ringkasan "kurang N petani menuju target" dibaca terhadap angka ini. Konten **Bantuan** bab Dashboard diperbarui (cara membaca tabel cakupan, drill-down, catatan pre/post, Pelatihan tanpa snapshot). Gate lint 0 / build / **test 567** ✅. |
| 07-21   | **DASH-06 Dashboard Pelatihan ✅** — `/admin/dashboard/training` (menu `dashboard-training` order 3, VIEW 4 role) — **live query, bukan snapshot**: 5 KPI card (cakupan petani terlatih, kegiatan, kehadiran vs petani unik, partisipasi perempuan, kenaikan skor) + **matriks cakupan Lembaga × Paket** (heatmap sortable, collapsible) + chart tren stacked-bar per paket (bulan bila tahun dipilih, tahun bila semua) + panel efektivitas pre/post (menandai skor turun) + panel kualitas data ber-deep-link; filter Kategori/Distrik/Lembaga/Tahun di-slice client-side. RBAC 3 lapis (`requirePermission` + `hasPermission` di action + `farmerGroupAccessFilter`), `isActive` difilter di 4 level. Seed menu+permission ke DB (dry-run dulu) ✅. Gate lint 0 / build / **test 561** ✅. |
| 07-21   | **fix(master-data): format angka lokal `id-ID` pada card & tabel** — card ringkasan Lembaga Petani/Petani/Pelatihan dan kolom tabel (Total Petani, Total Persil, Luas Lahan, Luas lahan) sebelumnya merender angka mentah (`3121 orang`, `7746.99 Ha`) → kini `3.121 orang` / `7.746,99 Ha` via `Intl.NumberFormat("id-ID")`, mengikuti pola `dashboard/summary-cards.tsx`. |
| 07-20   | **Rilis `v0.14.0`** — MINOR: fase **RPT-05 Laporan Lahan** Done (#177 #179 #180) — roster per Lembaga (wajib) + **PDF landscape ber-peta poligon vektor** (grid index fleksibel Baris×Kolom, label ceklis No/Nama/ID Petani/ID Lahan/KT adaptif vertikal/auto-scale + anti-tumpang, skala batang + panah utara, mini-index sel) + **Excel multi-sheet ber-gambar peta** + preview SVG on-page; field lahan baru **`species` + `isPsr` (PSR)** + data fix Komoditas "Kelapa Sawit" 4.163 lahan (migrasi `20260720044357`); TD-018 (dedup dropdown report) & TD-019 (exporter build-vs-save + test struktural) ✅. Gate 519 test. Bump `package.json` `0.13.0` → `0.14.0`; PR `mvp` → `main` + tag annotated `v0.14.0` + GitHub Release dengan notes dari changelog. |
| 07-17   | **Rilis `v0.13.0`** — MINOR: **Peta BMP Layer 2 Produktivitas (Ton/Ha) per persil (MAP-03, #174)** — panel 2 section layer ber-radio + selektor Tahun/Rata-rata + 5 kelas warna, cetak PDF & Excel WYSIWYG ikut layer aktif, realtime client-side tanpa migration; termasuk 10 perbaikan review multi-agent (PDF legend wrap + cp1252-safe, popup anti-basi, sanity window tahun, sentralisasi metadata kelas, perf geojson) + fix(training) relabel paket MK "Manajemen Kelompok" & catatan import pelatihan 4 ICS. Bump `package.json` `0.12.0` → `0.13.0`; PR `mvp` → `main` + tag annotated `v0.13.0` + GitHub Release dengan notes dari changelog. |
| 07-17   | **#174 ✅ kode — Peta BMP Layer 2: Produktivitas (Ton/Ha) per persil (MAP-03)** — panel 2 section layer ber-radio, selektor Tahun/Rata-rata, 5 kelas warna, popup + cetak PDF/Excel WYSIWYG ikut layer aktif (tabel produktivitas per lahan); realtime client-side tanpa migration/query baru; +15 unit +1 perf test (suite 480); 10 temuan review multi-agent diperbaiki (PDF wrap+encoding, popup refresh, sanity tahun, sentralisasi metadata kelas, perf geojson). |
| 07-17   | **Import data pelatihan 3 ICS baru + revisi FORTASKI** — KP KBM 74 activity/2.454 participant, APKSSB 31/1.147, KUD Karya Sembada (KSM) 15/883; FORTASKI 50→39 activity (946) — modul **P&C & NKT digabung satu kegiatan**; paket MK di-relabel **"Manajemen Kelompok"** (DB+UI+seed+docs); dashboard cache di-refresh. |
| 07-16   | **Rilis `v0.12.0`** — MINOR: gabungan seluruh `feat:` 2026-07-16 dalam **satu rilis akhir hari** (aturan baru maks. 1 rilis/hari): sertifikasi ISPO + Assurance SAP/MAP + card & badge sertifikasi Main Dashboard (#169), form Lembaga Petani 5 section + fix label Select (#170), **detail Lembaga Petani profil 360°** (#171), **detail Petani profil 360°** (#172), sensor NIK & tanggal lahir di layar, polesan layout Main Dashboard. Bump `package.json` `0.11.0` → `0.12.0`; PR `mvp` → `main` + tag annotated `v0.12.0` + GitHub Release dengan notes dari changelog. |
| 07-16   | **Sensor data pribadi petani di layar** (keputusan owner) — `lib/mask.ts`: **NIK** tampil 4 digit depan + 2 belakang sisanya bintang (`maskNik`); **tanggal & bulan lahir disensor, tahun tampil** ("** *** 1980", `maskBirthDate`); `maskIfNik` untuk string NIK-like (10–16 digit). Diterapkan di semua tampilan layar: detail Petani (#172, badge Valid NIK dihapus sekalian), list Petani (kolom NIK + Tanggal Lahir), Report Petani (kolom NIK), detail Pelatihan (NIK peserta), DA-02 (detail anomali NIK-like). **Excel/PDF export sengaja tetap nilai penuh** — hasil export bisa diedit lalu di-upload ulang via bulk; nilai ter-sensor akan merusak data. +3 unit (test **464**). |
| 07-16   | **#172 Detail Petani: profil 360° ber-Tabs ✅ kode** — `/admin/master-data/farmers/[id]` dirombak (pola #171, satu fase penuh): **header** (avatar placeholder inisial TD-017 + badge gender/status + **badge Lembaga ber-link ke detail #171** + KT/Gapoktan #152 + Edit per-permission) + **5 summary cards** (Lahan+Luas · Produksi · Pelatihan n/paket · **Kelengkapan Profil** 5-cek [NIK valid regex DA-02/alamat/tempat lahir/tgl lahir/tahun gabung, sub-teks daftar yang belum] · **Produktivitas terakhir** #166) + **Tabs**: Ringkasan (profil + umur + indikator NIK valid + meta audit), Lahan (tabel persil ber-revisi + **tombol PDF "Profil Lahan"** per lahan via action baru `getFarmerParcelPassport` ber-guard menu petani + **peta sebaran** shared), Pelatihan (**checklist paket wajib** ✓/✗ ber-jumlah partisipasi + riwayat ber-skor pre→post), Produksi (per tahun ber-persentase + bulanan collapsible + 4 kategori). **Refactor reuse**: `GroupParcelsMap` → `components/shared/parcels-distribution-map.tsx` (dipakai #171+#172); agregasi produksi per-tahun/bulanan → `lib/production-stats.ts`; fetch Farm Passport → `lib/parcel-passport-query.ts` (map.ts & farmer.ts memanggil lib yang sama, guard masing-masing). Action `getFarmerDetail` RBAC + scope by-id; pure lib `farmer-detail.ts` +4 unit (test 457→**461**). **Breadcrumb override** (revisi owner): halaman detail ber-`[id]` menampilkan label human-facing di segmen terakhir — Petani = **ID Petani**, Lembaga = **nama Lembaga** — via store mini `breadcrumb-override.tsx` (CUID URL tak informatif). **Persen Record produksi disesuaikan** (revisi owner): = **kelengkapan pelaporan bulanan** — distinct lahan×bulan melapor ÷ (total persil × 12) karena pelaporan wajib min. 1 panen/bulan per lahan (field `reportedParcelMonths` di `production-stats.ts`; berlaku di detail Petani & Lembaga). Gate lint 0 / build / test 461 ✅. |
| 07-16   | **#171 Detail Lembaga Petani: profil 360° (Fase 1) ✅ kode** — halaman detail `/admin/master-data/groups/[id]` dirombak: **header** (nama+kode+badge sertifikasi RSPO/ISPO/SAP-MAP pola #169 + status + tombol Edit per-permission) + **5 summary cards** (Petani L/P · KT & Gapoktan/Blok · Persil+Luas · Produksi Ton+rentang tahun · **Skor Kelengkapan Data DA-02** card+link) + **Tabs**: Ringkasan (profil lengkap + meta audit + **Struktur Kelembagaan** reuse `buildKelompokTaniDetailReport` #154 dengan **auto-hide kolom Gapoktan/KUD** bila Lembaga tak punya level itu + link roster), Petani & Lahan (ringkas — Fase 2 pending data; **tab Lahan + peta sebaran poligon semua lahan** `GroupParcelsMap` (permintaan owner saat review): warna per **Kelompok Tani** (palet 12 warna, normalisasi #154, tanpa-KT abu), **legenda kiri-atas ber-checklist show/hide per KT** + tombol Tampilkan/Sembunyikan semua + **Zoom ke tercentang**, **label nama petani dalam poligon** (zoom-aware, reuse `parcelLabelFit` Peta Lahan) + **popup klik** (petani/kode/KT/Gapoktan/Blok/luas), fit-bounds + style selector, tinggi 768px), Pelatihan (cakupan per paket + **rataan pre & post test per paket** + daftar aktivitas + rata-rata pre→post per aktivitas), Produksi (per tahun: total/record/lahan & luas melapor **ber-persentase** [record thd total record semua tahun; lahan/luas thd total persil/luas Lembaga]/**produktivitas Ton/Ha** #166 + **rincian bulanan collapsible** per tahun [klik baris tahun → baris per bulan; produktivitas hanya level tahun] + **distribusi 4 kategori ketersediaan** MAP-02 + link silang). Backend: action `getFarmerGroupDetail` (RBAC 3 lapis, scope by-id `AND`, 3 query paralel tanpa N+1, **real-time** per keputusan #153/#154) + pure lib baru `farmer-group-detail.ts` (+5 unit, test 452→**457**); `computeCompleteness` DA-02 dipanggil langsung (bukan via action report/DA — beda permission menu); `MAP_STYLES` di-export dari `parcel-map-view`. Gate lint 0 / build / test 457 ✅. |
| 07-16   | **#170 Form Lembaga Petani: field dikelompokkan per section** — dialog diperlebar (`sm:max-w-[640px]`, scroll `max-h-[85vh]`) + 15 field dikelompokkan 5 section ber-heading: **Identitas** (Nama; Kode/Singkatan/Abrv 3ID) · **Klasifikasi** (Distrik/Kategori/Tipe Grup) · **Tahun** (Berdiri + Bergabung berdampingan) · **Sertifikasi & Assurance** (3 baris seragam RSPO/ISPO/SAP-MAP: label skema + Status + Tahun) · **Lokasi** (Lat/Long). **Fix menumpang:** trigger `Select` menampilkan nilai mentah ("NONE"/"SWADAYA"/id distrik) karena Base UI me-render raw value → prop `items` (mapping value→label) di semua Select form; input tahun +placeholder & `min`/`max` 1900–2100. Markup-only — payload/name/Zod tidak berubah. Gate lint 0 / build / test 452 ✅. |
| 07-16   | **Polesan layout Main Dashboard (1 layar)** — opsi card kompak dicoba lalu dibatalkan owner; pilihan final: **pangkas area peta** (min-h 420→360px; konten info panel 360→300px), porsi **peta : info panel 60:40** (grid-cols-5 span 3:2, semula 75:25), **konten info panel 2 kolom** (statistik \| cakupan pelatihan; header judul/kode/badge tetap full-width; label "Cakupan Pelatihan" dihapus agar seimbang). Commits `b740fff`/`62f5ab1`/`446bd11`/`dfc1353`. |
| 07-16   | **#169 Lembaga Petani: Sertifikasi ISPO + Assurance SAP/MAP + 3 card sertifikasi di Main Dashboard** — 4 kolom nullable `FarmerGroup` (migrasi `20260716031500`, **applied** approval owner): `ispoCertYear`+`ispoCertStatus`, `sapMapAssuranceYear`+`sapMapAssuranceStatus` (enum generik baru `CertStatus` CERTIFIED/PLANNED; `RspoCertStatus` tetap). Zod: aturan seragam 3 skema (status boleh tanpa tahun; tahun tanpa status ditolak). Formatter disatukan `formatCertStatus` (`farmer-group-labels.ts`) + `formatIspoCert`/`formatSapMapAssurance`; form modal +2 baris field; list +2 kolom (dengan `sortValue` + export key terverifikasi — TD-015) + Excel; detail +2 field. **Main Dashboard +3 card** (Sertifikasi RSPO/ISPO/Assurance SAP-MAP: "N lembaga" tersertifikasi + sub "· plan"; **posisi setelah card Total Kelompok Tani**, revisi owner) + **badge sertifikasi di info panel Lembaga** (di bawah kode; filled = Tersertifikasi, outline = Plan, dengan tahun — `KTDetails` menyimpan status **+ tahun** per Lembaga) — snapshot-backed: status per Lembaga di `KTDetails`, rekap `certStats` di `DashboardStats`, dihitung ulang oleh `sumKelompokTaniStats` saat scoping/filter client-side; `normalizeSnapshotData` default 0 (snapshot lama → card 0 sampai **regenerate snapshot**); year-independent. +11 unit (Zod/formatter/agregasi; test 441→**452**). Gate lint 0 / build / test 452 ✅. |
| 07-15   | **Rilis `v0.11.0`** — MINOR: filter **Kelengkapan Data** di BMP Dashboard (`feat:` sejak `v0.10.0`). Bump `package.json` `0.10.0` → `0.11.0`; PR `mvp` → `main` + tag annotated `v0.11.0` + GitHub Release dengan notes dari changelog. |
| 07-15   | **[DASH-04] Filter Kelengkapan Data di BMP Dashboard** — masalah: banyak bulan kosong membiaskan Rataan/produktivitas. Filter global ke-5 di sebelah Tahun: **Semua Data ⇄ Data Full 1 Tahun** (hanya lahan ber-data 12 bulan penuh Jan–Des pada tahun ybs — per lahan per tahun; tahun berjalan belum bisa full). Snapshot +subset `monthlyFull`/`byYearFull` per Lembaga (`buildBmpSnapshotData` 2-pass); `sumBmpGroups`/`bmpStatsForYear` +param `dataMode`; sub-teks cards menyebut mode; record tanpa lahan tak pernah masuk subset full. **Wajib regenerate snapshot BMP** (snapshot lama → mode Full tampil 0). +5 unit (test 441). |
| 07-15   | **Import produksi KBM (data owner, 2 file rekap)** — Apr24–Mar25: 4.695 record / 10.651.323 kg; 2026 (Jan–Mei, per 13 Mei): 1.585 record / 3.288.420 kg. Hanya baris **full match** (petani 100%, lahan aktif); 107 kavling CSV belum ada di master (sufiks .B/.C dsb., 17,5%/16,4% produksi) **ditunda** menunggu update master lahan — daftar di Excel `scripts/local/seed/data-produksi/analisa-kbm-produksi*-vs-db.xlsx`. Script idempotent `scripts/local/import-produksi-kbm*.ts` (skipDuplicates, cek kepemilikan petani↔lahan, 0 mismatch). |
| 07-15   | **Rilis `v0.10.0`** — MINOR: phase **DASH-04 (BMP Dashboard Produksi, #166) Done**. Bump `package.json` `0.9.0` → `0.10.0`; PR `mvp` → `main` + tag annotated `v0.10.0` + GitHub Release dengan notes dari changelog. |
| 07-15   | **#166 DASH-04 BMP Dashboard (Produksi) ✅** — `/admin/dashboard/bmp` snapshot-backed: 4 score cards produksi (Produksi / **Produktivitas Ton/Ha per tahun** / Lahan ber-data / Petani melapor), combo chart SVG produksi + % lahan melapor (sumbu Y adaptif tick bulat, stretch mengisi card), panel Ketersediaan Data Produksi 4 kategori (reuse MAP-02) + **filter global Distrik/Lembaga/Kategori/Tahun** client-side; model `BmpDashboardSnapshot` (migration `20260715081831` applied + seed menu/permission, approval owner; fix CRLF `menu.csv`) + `lib/bmp-dashboard-aggregation.ts` (grain per-Lembaga `monthly`+`byYear`) + actions `snapshot-bmp.ts`/`dashboard-bmp.ts` + tools `/admin/tools/snapshot-bmp`; +27 unit +2 perf (test 435). Monev BMP follow-up. |
| 07-15   | **Rilis `v0.9.0` — rilis SemVer pertama + standar versioning** — dokumen baru `standards/versioning.md` (skema SemVer aplikasi, kriteria bump MAJOR/MINOR/PATCH, alur rilis 6 langkah + checklist); `package.json` `0.1.0` → `0.9.0`; tag annotated `v0.9.0` di `main` + GitHub Release dengan notes dari ringkasan changelog. Lihat Decision Log 2026-07-15 untuk alasan pemilihan angka `0.9.0`. |
| 07-15   | **#163 Perf load list full app — select ramping & agregasi DB** — audit 22 server actions; fix 6 modul: **Lahan** & **Produksi** (buang `geometry` dari payload list — penyebab dominan), **Petani**, **Pelatihan** (select ramping; `evidenceKey/Name` tetap, round-trip form), **Lembaga Petani** (agregasi petani/persil/luas → `groupBy` DB), **Region** (tree 4 level select id/code/name/isActive). Form edit Lahan berhenti mengirim geometry; `updateLandParcel` pertahankan polygon bila field tak dikirim. Modul lain diaudit & sudah lean (Map/Dashboard/Report/DA/Settings/bulk-upload). Aturan "Payload Trimming" → `performance.md`. +4 unit (`farmer-group.test.ts` baru). Gate lint 0 / build ✅ / **test 417** ✅. |
| 07-14   | **#155 Relabel "Lembaga Tani" → "Lembaga Petani" (revisi manajemen)** — "Tani" kata kerja → noun "Petani". Sweep **hanya "Lembaga Tani"**: 168 label di `src/**` + `menu.csv` + baris menu DB (`tbl_menu_item` key `master-data-groups`) + 59 di docs. **"Kelompok Tani" tetap** (istilah resmi, = `subGroupLv2`); identifier `FarmerGroup` tetap. Gate lint 0 / build ✅ / test 388 ✅. |
| 07-14   | **#149 Master Lahan: kolom Gapoktan/Kelompok Tani/Blok di list + Excel** — 3 kolom baru (`subGroupLv1`/`subGroupLv2`/`blok`) di `parcel-list-client.tsx`, **default hidden** (toggleable via column-visibility), ikut Excel export saat divisibilitaskan (export mengikuti kolom aktif). Bonus fix: kolom **"Lembaga Petani" export kosong** — `getExportRow` key `groupName`→`farmerGroupName` agar cocok dengan `col.key` (exceljs map by-key). Filter list by KT/Gapoktan (opsional) **ditunda** — data `subGroupLv2` belum terisi. Gate lint 0 / build ✅ / test 381 ✅. |
| 07-14   | **Hierarki kelembagaan: relabel "Kelompok Tani" → "Lembaga Petani" + sub-kelompok interim per-lahan + field blok (#146, #147)** — Koreksi mislabel: entitas `FarmerGroup` secara semantik = **Lembaga Petani** (hierarki **Petani → Kelompok Tani (Gapoktan) → Lembaga Petani**). **(1) #146 interim:** sub-kelompok disimpan denormalisasi **di `LandParcel`** (`subGroupLv1` Gapoktan + `subGroupLv2` Kelompok Tani, nullable) — **per-lahan** (satu petani bisa punya lahan di KT berbeda), bukan di `Farmer`; migrasi additif `add_land_parcel_sub_group`; form + detail lahan. **(2) #147 relabel:** sweep ~56 file `src/**` + `menu.csv` + **label menu DB** (1 baris terarah `tbl_menu_item`) "Kelompok Tani"→"Lembaga Petani"; label UI "KT" (Cari/Nama/Total/Kode/Profil/tab data-access dst) → "Lembaga Petani"; **dipertahankan** identifier `FarmerGroup`/`farmer_group`/`BY_FARMER_GROUP`, menu-key, kode fase/#issue, dan field `subGroupLv2` (KT asli); "KT" tak di-mass-replace (risiko "NKT"). **(3) Field `LandParcel.blok`** (String?, blok kebun) + migrasi additif `add_land_parcel_blok`. **(4) Fix** filter Tahun dashboard menampilkan "all" → "Semua Tahun" (base-ui `SelectValue` function-child). Docs disinkronkan (models/erd Schema 2.5.0/migrations/code-standards + relabel). **Gate:** lint 0 error / build ✅ / test 380 ✅. |
| 07-13   | **BUG-007 fix — scope-leak `getMapData` (MAP-01)** — follow-up #144: `farmerGroupAccessFilter(access)` di `groupWhere` dipindah **spread → `AND`** (samai `getBmpMapData`) agar literal `districtId`/`id` tak menimpa scope `{ …: { in } }` → user ber-scope tak lagi bisa muat District/KT luar assignment via action langsung. +3 replica test (`map.test.ts` 31→34). `npm run lint` 0 error, `npm run build` ✅, `npm test` **380 pass**. TD BUG-007 → ✅ Done. |
| 07-13   | **MAP-02 Peta BMP — Layer 1 Ketersediaan Data Produksi (#144)** — sub-menu peta **kedua** stream MAP: `/admin/map/bmp`, mewarnai tiap **lahan** menurut kelengkapan data produksinya. **4 kategori** dari **run bulan berturut-turut terpanjang** `ProductionRecord.period`: Baik >24 bln (hijau) · Cukup 12–24 (kuning) · Kurang 1–11 (oranye) · Tidak ada 0 (abu). **Lembaga Petani wajib** (Provinsi/Distrik opsional, hanya menyaring KT; bisa pilih KT langsung); Muat Data disabled sampai KT dipilih. Server action `getBmpMapData` (RBAC 3-layer `map-bmp` VIEW + scope KT + `isActive`); produksi via **1 `productionRecord.groupBy` scoped `parcelId IN [...]`** (hindari N+1). Helper murni `longestConsecutiveMonths`/`productionAvailabilityCategory` (+ambang `BMP_BAIK_MIN_MONTHS=24`/`BMP_CUKUP_MIN_MONTHS=12`) + `buildBmpMapData` di `map-data.ts`; tipe + Zod `bmpMapFilterSchema`. Canvas MapLibre **data-driven color** (`match` per kategori) + point KT konteks + popup (badge kategori, run bulan, periode awal–akhir) + legend 4-kategori toggle+count. **File terpisah** (tak menyentuh Peta Lahan). Produksi tanpa `parcelId` diabaikan (disclaimer; #136). Read-only, **tanpa tabel/migration baru**. **Revisi desain owner:** (1) rendering **poligon saja, tanpa titik** — centroid lahan & titik KT dibuang; **NONE = outline saja** (base, tanpa fill), Baik/Cukup/Kurang = fill berwarna + outline (`fill-opacity` `match`: NONE→0). (2) **Tombol Cetak Peta** → PDF **A4 landscape** (jsPDF + jspdf-autotable, lazy-import): **hal.1** = judul KT + snapshot gambar peta + legend + count (`canvasContextAttributes.preserveDrawingBuffer` maplibre v5; snapshot via `map.once("render")`+`triggerRepaint`; gagal-anggun bila basemap **Hybrid/Google** canvas tainted → toast sarankan Light/Dark); **hal.2+** = **matriks ketersediaan data per lahan × bulan** (autotable, sel = **total kg** bulan itu + latar hijau muda via `didParseCell`, header bulan 2-baris per tahun, kolom `enumeratePeriods`). (4) **Download Excel** matriks (`lib/xlsx.ts` `exportToExcel`, kolom bulan `MMM-YY` dinamis, lazy-import exceljs) — tombol "Download Ketersediaan Data (Excel)"; tombol cetak di-rename "Cetak Peta dan Matriks Ketersediaan Data". Server `getBmpMapData` `groupBy` +`_sum:{yieldKg}` (biaya query sama, tanpa N+1) → field `BmpParcelFeature.production` (period→kg); test buildBmpMapData disesuaikan (input `{period,kg}[]`, +assert `production` sum duplikat). (5) **Popup dijadikan accordion** (pola Peta Lahan): section **Detail Lahan** + section **Produksi Bulanan** = grafik bar dengan selektor Rata-rata/per-tahun, dihitung client-side dari `production` yang di-embed ke feature (JSON string, karena properti MapLibre tak bisa objek) via `summarizeProduction` — **tanpa fetch tambahan** & tanpa coupling guard `map-parcel`. (6) **Label nama petani** di dalam poligon (reuse `parcelLabelFit`/`geomBounds` map-geo.ts — tampil hanya bila teks muat pada zoom aktif, ikut `categoryFilter` & warna basemap). (7) **Audit rule-compliance:** ditemukan & **diperbaiki scope-leak key-collision** di `getBmpMapData` — `farmerGroupAccessFilter(access)` dipindah dari spread ke **`AND`** agar literal `id`/`districtId` tak menimpa `{ id/districtId: { in } }` (BY_FARMER_GROUP/BY_DISTRICT), sesuai pitfall `code-standards.md`/#127; **pola sama masih ada di `getMapData` MAP-01 → dicatat BUG-007** (menunggu approval, di luar scope #144). **+2 perf test** (`perf.test.ts`: `buildBmpMapData` 500 lahan × 36 bulan = **9,27ms** <200ms; `longestConsecutiveMonths` 600 bulan = **0,066ms** <10ms). (8) **Tombol "Zoom ke semua data"** (ikon `Maximize`, `fitBounds` ke semua fitur terlepas toggle) ditambahkan di **MAP-02 & MAP-01** (Peta Lahan) + **diresmikan jadi aturan** di `ui-ux.md` §Geospatial (wajib pada tiap menu peta full-bleed, reuse `fitAll()`). (9) **Keseragaman kontrol peta:** basemap switcher + Zoom-ke-semua dipindah ke **kanan-bawah** di MAP-01 (samai MAP-02); **panel kiri MAP-01 dibuat minimizable** ke ikon (pola sama BMP) — kedua pola diresmikan di `ui-ux.md`. (10) **Excel ketersediaan data:** +2 kolom setelah ID Lahan — **Status Ketersediaan Data** (Baik/Cukup/Kurang/Tidak ada Data) & **Luas Lahan (Ha)**. **Gate:** lint 0 error, build ✅, **test 27 file / 377 ✅** (dari 375). (3) **Panel kanan floating minimizable** (`map-bmp-data-panel.tsx`, + search) = matriks **ketersediaan data per lahan × bulan** (blok emerald = ada record bulan itu, kosong = tidak; kolom bulan dikelompokkan per tahun dari rentang data, reuse `enumeratePeriods` RPT-03) + kolom **Zoom to** (fitBounds `geomBounds` + buka popup); basemap switcher dipindah ke kanan-bawah. Field `farmerCode` + `periods` ditambah ke `BmpParcelFeature`. Seed 2 baris CSV **sudah dijalankan** (approval owner, `seed-menu-only.ts`, upsert). +16 unit test (`map.test.ts` 15→**31**). `npm run lint` 0 error, `npm run build` ✅, `npm test` **27 file / 375 pass** ✅. |
| 07-11   | **MAP-01 Produksi Peta & PDF (#134) + Panel Daftar Lahan (#135)** — popup Peta Lahan buang grafik dummy → **produksi asli per-lahan** (`getParcelProduction`, RBAC 3-layer, by `parcelId`) + **selektor Rata-rata/tahun** (sumbu-Y kanan + tooltip). **Farm Passport PDF** → **tabel matriks tahun×bulan×Total** & rebrand **"Profil Lahan"** (header/file/tombol/toast). Helper murni `summarizeProduction` (`map-data.ts`) + tipe `ProductionSummary`. Fix **popup tidak refresh** saat ganti lahan (`key` per fitur). **Dedup fetch produksi** popup↔PDF (`getParcelPassport(includeProduction)` + `ParcelPopupBody` shared state). **#135:** panel kanan Daftar Lahan (search nama/ID + zoom ke lahan). **Legenda** dibuat collapsible. +4 unit (`map.test.ts` 15) + 1 perf (`perf.test.ts` 8; ~60k rec/1 lahan 9ms). Tech debt → **#136**. `npm run build` ✅, `npm test` **311 pass** ✅. |
| 07-10   | **AUDIT MENYELURUH + sinkronisasi docs** — Audit folder/file/code terhadap rule di `docs/`: QA (test 24/296 ✅ · build ✅ · lint ❌ 190 error), matriks kepatuhan 22 server actions, 35 pages, hygiene/deps, drift docs. 6 temuan HIGH → BUG-003/004/005/006 + TD-009…TD-012 didaftarkan; compliance table diperbarui (7 PASS/4 PARTIAL/3 FAIL); `database-schema.md` (+4 migrasi, unique ProductionRecord, index/FK snapshot), `rule.md` (shpjs, arsitektur, revision default 0, pengecualian barrel `@/components/shared`, catatan font), `ui-ux-flow.md` (angka test/fase, menu aktual, blok legacy duplikat dihapus), `progress.md` (angka & kontradiksi internal, struktur section 6 dirapikan). Laporan: `audit-report/audit-2026-07-10.md` (internal, gitignored). **6 GitHub issue dibuat: #125 (P0 keamanan RBAC), #126 (P1 lint), #127 (P1 scope by-id + pola restore), #128 (P1 konvensi UI), #129 (P2 cleanup deps/dead code), #130 (P3 kualitas berkelanjutan).** |
| 07-10   | **MAP-01 enhancement — Hotspot · Ruler · Label KT/Petani** (permintaan langsung, di luar #113). Layer **Titik Api (Hotspot)** NASA FIRMS VIIRS 375 m: proxy `api/map-hotspot` (**auth-guarded**, cache 1 jam, key server-only), **bbox Riau**, window **24 jam / 5 hari** (batas FIRMS `[1..5]`), warna by kebaruan + popup detail + disclaimer + atribusi. **Tool Ruler**: jarak & luas **geodesik** (haversine/spherical), label segmen, fill poligon, undo/Esc. **Label**: nama KT (titik) + nama petani (poligon, **hanya bila muat**, wrap otomatis, bounds precomputed per-zoom). Helper murni `lib/firms.ts` + `map-geo.ts`; **+22 unit test** (`firms.test.ts` 10, `map-geo.test.ts` 12). Improvement pasca-audit: auth guard route, attribution NASA FIRMS, optimasi label. Perf: route cold ~1,7s (FIRMS-bound) / warm ~5ms (cache), payload 61 titik ~15KB. `npm run build` ✅, `npm test` **296 pass** ✅. |
| 07-09   | **DA-02b complete (#122)** — Enhancement Domain Pelatihan pada DA-02 (modul & route **sama**, tanpa menu/permission baru). Aturan bisnis: setiap petani wajib mengikuti **seluruh paket pelatihan** wajib (`ref_training_package` aktif, **exclude OTHER**; kehadiran cukup, nilai pre/post-test tidak memengaruhi cakupan; partisipasi hanya dihitung untuk activity **KT ini**). Section Pelatihan kini menampilkan cakupan **per paket** dengan collapse **bersarang per analisa**: **4a Ringkasan per Paket** (kartu per paket + nested daftar petani belum ikut), **4b Matriks Cakupan** (petani × paket, ✓/✗, sticky header/kolom, bounded 50 baris), **4c Petani Belum Lengkap** (paket yang masih kurang, urut cakupan terendah). Skor Domain Pelatihan diubah ke **rata-rata % cakupan paket per petani** (ikut memengaruhi skor kesehatan KT). Export Excel DA-02 +2 sheet (Matriks Pelatihan, Petani Belum Lengkap). **UI konsistensi:** nested collapse **per anomali** juga diterapkan ke domain lain (Petani/Lahan/Produksi) via `SubCollapsible` (default tertutup, badge count terlihat). Perubahan surgical di `data-completeness.ts` (fetch paket + participant map), pure logic `computePelatihanDomain` (cakupan/matriks/belum-lengkap), types, dan client — logika domain lain tak berubah. +8 unit test (`data-completeness.test.ts` 23→31) + 2 perf test (`computePelatihanDomain` 5.000 petani × 4 paket ~13ms; `computeCompleteness` 5 domain 5.000 petani ~11ms). `npm run build` ✅, `npm test` **274 pass** ✅. |
| 07-08   | **MAP-01 Farm Passport PDF** — tombol "Print Profil Petani" di popup lahan → `getParcelPassport` (RBAC-scoped) + `src/lib/farm-passport.ts` (jsPDF A4): identitas petani, layout lahan (polygon vektor dari GeoJSON), Data Petani + Informasi Lahan, tabel Pelatihan, dan grafik Produksi rata-rata bulanan **dari data asli `ProductionRecord`** (`monthlyAverageYield`, 4 unit test). jsPDF di-lazy-import. Build ✅, 231 test ✅. |
| 07-08   | **MAP-01 selesai (#113)** — stream baru `MAP`. Menu **Map** + **Peta Lahan** (`/admin/map/parcel`). Peta full-bleed MapLibre (`react-map-gl`) + panel filter floating kiri collapsible (Province→District→KT + Muat Data, auto-collapse saat load) + legend layer toggle (Point KT / Point centroid lahan / Area polygon lahan + count). Klik feature → info popup: KT (nama/kode/distrik/koordinat); Lahan = accordion **Detail Lahan** + **Pelatihan Petani** (lazy-load per petani, `getFarmerTraining`, 3-layer RBAC) + **Produksi** (grafik rata-rata bulanan Jan–Des, **dummy scaffolding**). Read-only `FarmerGroup`+`LandParcel` (tanpa tabel/migration baru); centroid lahan dihitung via `@turf`. `map.ts` (`getMapData` + dropdown fetchers + `getFarmerTraining`) + `map-data.ts` (pure, 7 unit test) + types + Zod. Seed menu+permission via `scripts/local` (upsert, DB tidak di-reset). Build ✅, 20 files / 227 test ✅. Follow-up: data produksi asli + Recharts. |
| 07-08   | **Issue #113 [MAP-01] dibuat** — stream baru `MAP`. Menu **Map** + sub-menu **Peta Lahan** (`/admin/map/parcel`, scaffolding): peta full-bleed MapLibre (`react-map-gl`) + panel filter floating kiri (Province/District/KT + Muat Data, collapsible) + legend layer toggle (Point KT / centroid lahan / polygon lahan) + info floating on click. Read-only `FarmerGroup`+`LandParcel`, tanpa tabel baru, centroid via `@turf`. RBAC 3 layer. Belum diimplementasi (Not Started). |
| 07-08   | **Issue #99 DASH-01: Dashboard Snapshot complete** — Menu `dashboard` (parent) + `dashboard-main` (Main Dashboard, `/admin/dashboard/main`) + `tools` (parent) + `dashboard-snapshot` (`/admin/tools/snapshot`). **Main Dashboard bersifat snapshot-backed**: membaca snapshot terakhir dari `tbl_snapshot_main_dashboard` (bukan real-time), subtitle menampilkan "Nilai di bawah di-generate pada dd-MMM-yy HH:mm"; jika belum ada snapshot → empty state + link ke Tools. Dashboard memuat **1 snapshot master (Semua Distrik/Semua Tahun)**; filter di header (kanan sejajar judul) **Distrik + Tahun Bergabung + Lembaga Petani** menyaring isi snapshot **sepenuhnya client-side** (recompute cards/peta/info panel, tanpa panggilan server per filter). Snapshot menyimpan per-KT `districtId` + `byYear` (rincian petani/gender/persil/luas/cakupan per tahun bergabung), helper `ktStatsForYear`. **10 summary cards (5 per baris)**: baris 1 = Total KT, Total Petani, Petani Laki-laki, Petani Perempuan, Total Persil; baris 2 = Total Luas, Paket 1 BMP/NKT/RSPO, Paket 2-MK, Paket 2-HSE, Paket 3&4 GEDSI/BUSDEV (revisi management: tambah gender). Peta MapLibre (`react-map-gl`) clustering marker KT, basemap light/dark/hybrid (default ikut tema), search KT + tombol "Lihat Semua", info panel per-KT. Snapshot: model `MainDashboardSnapshot` → tabel `tbl_snapshot_main_dashboard` (migration additive), **generate hanya lewat Tools**, menyimpan data flat `DashboardSnapshotData` (incl. gender) + `normalizeSnapshotData` untuk baca snapshot lama, server actions `dashboard.ts` (baca snapshot) + `snapshot.ts` (generate/list/detail/delete, 3 layer RBAC), lib `dashboard-aggregation.ts` (pure, teruji) + `dashboard-query.ts`, Excel export (incl. Petani L/P) + soft delete, PDF placeholder. 8 unit test baru (`dashboard.test.ts`). Build ✅, 19 files / 216 tests ✅. |
| 07-06   | **Issue #107 RPT-01: Report Petani complete** — Implementasi menu `report` dan `report-farmer`, server action `report.ts` dengan access context check, halaman laporan `/admin/report/farmer` dengan filter cascading wajib (tanpa "Semua"), 4 summary cards, tabel petani, tombol ekspor Excel & PDF (window.print() clean styles), serta 3 unit tests di `report.test.ts`. |
| 07-09   | **DA-02 improvements** — Setelah audit rule + performance test: (1) render tabel anomali dibatasi (bounded) 50 baris awal dengan tombol "Tampilkan semua" agar DOM tetap ringan pada KT besar (data penuh tetap via Excel export); (2) notice eksplisit saat KT tanpa petani aktif. Perf test (data seed): query Prisma ~180ms & `computeCompleteness` ~1ms untuk KT terbesar (248 petani); stress synthetic 5.000 petani → 6.7ms (logika murni bukan bottleneck). Optimasi query geometry & Zod-guard sengaja ditunda (Simplicity First — query 16× di bawah target 3s). `npm run build` ✅, `npm test` 254 pass ✅. |
| 07-09   | **Issue #118 complete** — Data Analyst (DA-02): Analisa Ketersediaan Data Lembaga Petani. Sub-menu baru `data-analyst-data-completeness` di bawah menu Data Analyst. Flow: pilih distrik → lembaga petani → Analisa. Menampilkan skor kesehatan data (tertimbang) + total anomali, lalu 5 section collapsible (Profil KT, Petani, Lahan, Pelatihan, Produksi) berisi kartu ringkasan + daftar entitas anomali per domain (NIK kosong/invalid/duplikat, petani tanpa lahan, persil tanpa geometry/luas, petani belum pelatihan, petani tanpa produksi, berlahan-tanpa-produksi, dst). Logika perhitungan murni di `src/lib/data-completeness.ts` (23 unit tests), server action dengan permission + scope-check AccessContext + filter isActive, export Excel multi-sheet. Registrasi ikon `ClipboardCheck`. |
| 07-06   | **3 GitHub Issues dibuat untuk Report module** — #107 [RPT-01] Report Petani: halaman `/admin/report/farmer`, filter distrik+KT wajib (no "Semua"), 4 summary cards, tabel petani + Export Excel, ~18 unit tests, estimasi 16–20 jam. #108 [RPT-02] Report Pelatihan: halaman `/admin/report/training`, 6 summary cards, 2 Tab (Kegiatan Pelatihan & Cakupan per Petani), Export Excel 2-sheet, exclude paket OTHER, ~16 unit tests, estimasi 18–24 jam. #109 [RPT-03] Report Produksi: halaman `/admin/report/production`, filter periode opsional (tahun/bulan), 4 summary cards, 2 Tab (Rekap per Petani & Detail Panen), Export Excel 2-sheet, ~18 unit tests, estimasi 16–20 jam. Semua issue menggunakan pola extend server action `report.ts` (RPT-02 & RPT-03 tidak membuat file baru). |

</details>

<details>
<summary><strong>Juni 2026</strong></summary>

| Tanggal | Perubahan                                                                                                        |
| ------- | ------------------------------------------------------------------------------------------------------------------- |
| 06-30   | **Issue #103 complete** — Data Analyst (DA-01): Ringkasan Petani module. Implemented 4 server actions in `src/server/actions/data-analyst.ts` with RBAC access context and hasPermission view validations. Implemented routing pages in `src/app/(admin)/admin/data-analyst/` displaying detailed statistics for active land parcels and listing farmers without active parcels. Configured cascading combobox filters, cards, client-side datatable with client pagination, fixed Excel download data mapping, and created 4 unit tests. |
| 06-30   | Master Data Pelatihan: Menambahkan kartu agregat statistik dinamis (Total Lembaga Petani, Total Kegiatan Training, Total Peserta, dan Total Peserta Unik) di atas tabel, menambahkan filter combobox baru untuk Paket Pelatihan pada toolbar kiri, serta menyembunyikan kolom status secara default. |
| 06-30   | Master Data Lembaga Petani: Menampilkan total petani, total persil, dan total luas lahan (Ha) di masing-masing kelompok pada tabel daftar lembaga petani, mendukung sorting, menyertakan data ini pada saat mengekspor data ke Excel, menambahkan kartu agregat statistik dinamis di atas tabel, memperbaiki tampilan label filter distrik agar menampilkan nama distrik (bukan ID), serta menyembunyikan kolom status secara default. |
| 06-26   | Issue #94 complete — Pre/Post-Test Scores: Tambah `preTestScore` & `postTestScore` (nullable Int 0-100) pada TrainingParticipant. Zod validation (`training-participant.schema.ts`), server actions (`updateParticipantScores`, `removeParticipants`), inline editing UI, bulk upload Excel/CSV parsing dengan score columns, bulk participant removal dengan checkbox selection. 7 unit tests. Audit: isActive filter fix, onBlur handler deduplication. |
| 06-25   | Issue #94 dibuat — Tambah field nilai Pre-Test dan Post-Test pada Training Participant: `preTestScore` & `postTestScore` nullable Integer (0-100), update schema, validation, server actions, UI form/table, bulk upload enhancement. Estimasi 4-6 jam (1 hari kerja). |
| 06-22   | **Scripts reorganization** — Debug/stale scripts dipindah ke `scripts/local/` (gitignored, local-only). `get-link.js` & `pdf-manager.js` tetap di `scripts/` root agar npm commands (`s3:get-link`, `pdf:*`) tetap berfungsi. BUG-002 & TD-005 resolved: stale scripts tidak ada di repo/CI. |
| 06-22   | Login Page Enhancement: Case-insensitive email login (`src/lib/auth.ts`) and show/hide password visibility toggle with eye icon (`src/components/auth/login-form.tsx`). |
| 06-22   | Menambahkan konfigurasi keamanan statis CI: Semgrep (`semgrep.yml`) dan Gitleaks (`gitleaks.yml`) untuk scan celah keamanan dan kebocoran rahasia (secrets). |
| 06-14   | MD-04 selesai — Implementasi lengkap master data Lahan / Land Parcels (#88): CRUD, UI list DataTable dengan filter & Excel export, UI detail dengan MapLibre polygon viewer, modal form dengan Zod validation, ZIP Shapefile bulk upload dengan column mapping & smart validation, 14 unit tests, menu seeding, dan full RBAC integration. |
| 06-11   | MD-05 — Upload evidence training (PDF), format tanggal dd/mmm/yyyy, serta import/upload list peserta via Excel/CSV dengan 3-tier validation (Valid, Warning, Error). |
| 06-14   | PLATFORM-07 selesai (#87) — Hierarchical Menu (3-Level): recursive tree building, validation depth max 3, cascading permission inheritance, sidebar 3-level collapsible styling, and updated Menu Management UI. |
| 06-12   | **CODE AUDIT COMPLETE** — Comprehensive audit seluruh codebase dan update keempat dokumen utama: progress.md (phase status verified), rule.md (compliance audit), database-schema.md (P0 sections), ui-ux-flow.md (compact collapsible format). Verified: 13 test files/155 tests ✅, Training MD-05 complete ✅, Bulk Upload BULK-03 complete ✅, 12 server actions/1600 LOC, 6 validation schemas, 8 Prisma models. Dashboard masih placeholder (DASH-01 scope needed). |
| 06-12   | Database Schema Documentation — Added P0 critical sections: Index Strategy (primary/secondary indexes dengan performance targets), Constraint & Data Integrity (FK behaviors, business rules, soft delete pattern, referential integrity flowchart), Migration Strategy (workflow, risk levels, migration history, pre-deployment checklist, breaking changes policy, data backfill examples), Security Considerations (auth/RBAC, password security, SQL injection prevention, data access patterns, sensitive data protection, audit trail, OWASP Top 10 compliance), dan Performance & Data Volume (3-year projections, table size estimates, query optimization, pagination strategies, N+1 prevention, connection pooling, caching strategy). Dokumentasi sekarang production-ready dengan 112 MB estimasi Year 3 dan query performance targets < 300ms. |
| 06-11   | Issue #87 dibuat (PLATFORM-07) — Hierarchical Menu Enhancement: support 3-level menu struktur (Menu Besar → Sub Menu → Detail Sub Menu) untuk Training Participants, Farmer detail pages (Parcel/Training/Production), dengan sidebar collapsible, RBAC inheritance logic, validation depth max 3, dan update menu management UI. Estimasi 12-16 jam. |
| 06-10   | MD-05 Training selesai (#77-#82) — Model schema, seed paket pelatihan, server actions CRUD + partisipan, antarmuka list DataTable, modal form Combobox, detail aktivitas, modal panel ganda penambahan peserta, dan 11 unit tests. |
| 06-10   | #86 selesai — Tambah field joinedYear (Tahun Bergabung) pada master data Petani: schema, validation Zod, CRUD, UI (form/list/detail), bulk upload column mapping, 4 unit tests baru, serta penambahan filter by District menggunakan searchable combobox. |
| 06-09   | #76 selesai — Implementasi Bulk Upload Petani (BULK-03): dynamic column mapping UI, smart validation, validation preview table, download full/error data Excel, dan bulk transactional save. |
| 06-09   | #68 selesai — Route Setup dan parent redirect `/admin/bulk-upload` ke `/farmers`. Menu seed updated.             |
| 06-06   | #71 selesai — Refactor tabel ke DataTable, menambahkan ekspor Excel dengan exceljs dan visibilitas kolom di list User & Lembaga Petani |
| 06-06   | Buat 9 GitHub Issues (#62–#70): Dashboard menu+cards, Report menu+placeholder+tabel+export, Bulk Upload menu+placeholder+CSV. |
| 06-08   | Buat GitHub Issue #76 (BULK-03): Bulk Upload Farmer dengan template-less approach, column matching UI, auto-mapping, validasi smart, preview, download invalid rows. Estimasi 20-28 jam (3-4 hari kerja). |
| 06-06   | Buat GitHub Issue #71 untuk DataTable refactor + Excel export. Tambah phase PLATFORM-06 ke progress.md. |
| 06-06   | Tambah stream RPT (Report) dan BULK (Bulk Upload) ke Phase Encoding Taxonomy dan Phase Status.                   |
| 06-06   | Update Sprint Focus, Active Issues, Dependency Map, dan Recommended Implementation Order.                        |
| 06-06   | Audit seluruh folder dan update `progress.md` berdasarkan source code aktual.                                    |
| 06-06   | Koreksi status dashboard dan master data lanjutan sesuai bukti route/schema/action yang ada.                     |
| 06-06   | Tambah Bug Register untuk broken redirect dan stale dashboard references.                                        |
| 06-06   | Validasi test lokal: `npm test` lulus 10 files / 111 tests.                                                      |
| 06-06   | Restrukturisasi `progress.md` agar setiap section collapsible dan siap untuk presentasi management dua mingguan. |
| 06-07   | Buat 4 GitHub Issues (#72–#75) untuk MD-03 Farmer: schema+migration, server actions+validation, UI (list/form/menu), docs. Estimasi 8-12 jam dev. |
| 06-07   | #72 selesai — `prisma/schema/farmer.prisma`: model Farmer + enum Gender + relasi FarmerGroup + seeder `seed-farmers.ts` dari CSV + menu entry Petani. |
| 06-07   | #73 selesai — `farmer.schema.ts` Zod (create+update), `src/server/actions/farmer.ts` CRUD+RBAC access context, `farmer.test.ts` unit tests. |
| 06-07   | #74 selesai — `farmers/page.tsx`, `farmer-list-client.tsx` DataTable+filter KT+Excel export, `farmer-form-modal.tsx`, `[id]/page.tsx` detail, `loading.tsx`. Menu CSV diupdate. |
| 06-07   | #75 selesai — `progress.md` diupdate: MD-03 Done, BUG-001 Done, Active Issues #72-75 Done, Snapshot, Audit Evidence, Changelog. |
| 06-07   | BUG-001 selesai — Redirect `/admin/master-data` diubah ke `/admin/master-data/farmers` (route sudah tersedia). |

</details>

<details>
<summary><strong>Mei 2026</strong></summary>

| Tanggal | Perubahan                                                                                                                                                 |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 05-25   | #61 selesai — User Menu Access Override: server actions, matrix override modal, RBAC helper caching & soft delete, integration, 111/111 tests             |
| 05-22   | #57 follow-up — Kolom ringkasan akses data di tabel User Management; bug fix RBAC KT-only; live refresh tabel saat toggle di modal; 105/105 tests         |
| 05-22   | #57 selesai — User Data Access Assignment: assign/remove Province/District/KT, modal tabs UI, visual hierarchy badges, live toggle, search, 104/104 tests |
| 05-22   | #59 selesai — Standardisasi visibilitas aksi tabel dan tombol Tambah berbasis Role & Permission, dokumentasi di `docs/rule.md`                            |
| 05-22   | #60 selesai — Abstraksi TableActions, TableSkeleton, loading state, dan pengamanan server actions dengan `hasPermission`                                  |
| 05-22   | #58 selesai — Region Management: tree view 4-level hierarchy, CRUD region, search, status filter, cascade muting                                          |
| 05-22   | #56 selesai — Login, User Management, Menu Management, Role & Permission matrix, Lembaga Petani CRUD, Profile page, 41/41 tests                            |
| 05-22   | #55 selesai — Schema reset, RBAC system, soft delete, audit trail, seed, migration fresh                                                                  |
| 05-13   | #48 — Update UI/UX Grafik BMP: filter kategori, grouped bar, warna hijau vibrant, legenda override                                                        |
| 05-13   | #48 — Dashboard BMP scaffold: score cards, combo chart, monev cards, filter distrik dan KT                                                                |
| 05-12   | Issues #48–#53 dibuat sebagai scaffold                                                                                                                    |
| 05-11   | #34 selesai — Dashboard full DB-driven: server actions, map controls, cache tables, 174/174 tests                                                         |
| 05-08   | #37 selesai — Interactive Map: filter KT, collapsible panel, icon markers, 100/100 tests                                                                  |
| 05-07   | #35 selesai — Dynamic Menu Management: Prisma, CRUD, sidebar, drag-and-drop, 95/95 tests                                                                  |
| 05-06   | #31 selesai — Sync production DB: 6 migrations, seed data                                                                                                 |
| 05-06   | #29 selesai — Audit trail 22 tabel, 81/81 tests                                                                                                           |
| 05-06   | #22 selesai — Final QA Fase 4: hapus debug, lokalisasi, cleanup placeholders                                                                              |
| 05-04   | Restrukturisasi dokumen, skip Fase 3, mulai Fase 4                                                                                                        |

</details>

<details>
<summary><strong>Koreksi Entri Historis (Mei 2026)</strong></summary>

Beberapa entri changelog Mei 2026 pernah mencantumkan status "selesai" untuk modul yang tidak ditemukan implementasinya di source code aktif. Status resmi sudah dikoreksi di **Phase Status**.

| Tanggal | Entri Historis yang Dikoreksi                                                                          |
| ------- | ---------------------------------------------------------------------------------------------------------- |
| 05-13   | Dashboard BMP scaffold/update tidak tercermin di source `/admin/dashboard`, yang masih `Coming soon`.  |
| 05-11   | Dashboard full DB-driven tidak tercermin di source aktif; tidak ada `src/server/actions/dashboard.ts`. |
| 05-08   | Interactive Map tidak tercermin sebagai route/component dashboard aktif.                               |
| 05-09   | #45 — Training PDF Management: hanya ada CLI S3/PDF, belum ada modul Training app/schema.              |
| 05-09   | #43 — Staff Activity: tidak ada Staff Activity model/route/action/UI.                                  |
| 05-09   | #41 — Staff WRI: tidak ada Staff model/route/action/UI.                                                |
| 05-08   | #39 — Training module lengkap: tidak ada Training model/route/action/UI.                               |
| 05-05   | #21 — Parcels CRUD + MapLibre view: tidak ada Parcel model/route/action/UI.                            |

</details>

<details>
<summary><strong>April 2026</strong></summary>

| Tanggal | Perubahan                                                                     |
| ------- | ----------------------------------------------------------------------------- |
| 04-14   | PLATFORM-02 selesai — Prisma 7 modular schema, 3 migrasi PostgreSQL + PostGIS |

</details>

<details>
<summary><strong>Maret 2026</strong></summary>

| Tanggal | Perubahan                                      |
| ------- | ------------------------------------------------ |
| 03-30   | Code review & sync status                      |
| 03-28   | Modernisasi Dashboard dan perbaikan Home       |
| 03-18   | Inisiasi proyek — Next.js, Shadcn, static data |

</details>
